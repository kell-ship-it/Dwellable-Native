# Dwellable Native — Full Ticket Registry

**September 3, 2026 session — Design-audit prep pivot: abandoned interactive click-through prototyping in favor of scenario-organized Figma pages, ready for an external designer's review; 3 tickets raised/updated (T-195 closed, T-202–T-203 new):**

**Pivoted away from prototyping mid-session.** After building substantial click-through prototype work across P0/P1/P4 (wired Figma prototype interactions, keyboard-zone voice recording states, entry-point flows), Kell judged further prototyping not fruitful ahead of bringing on an external designer — too slow to maintain, and duplicative once every scenario is already designed. New standing approach: each pillar's own Figma page organized into numbered "Scenario N — [Descriptive Title]" sections, each a horizontal row of screens read left-to-right, stacked top-to-bottom, with a description card per scenario (badge/title/description) — matching a convention Pillar 1 had already partially established. No further prototype click-wiring pursued after the pivot.

**All 10 pillar-facing Figma pages reorganized and marked "✅ Ready for Review" in their page titles:** P0 Onboarding (3 scenarios), P1 Capture Onboarding (12 scenarios, renumbered to resolve a collision between 4 happy-path and 8 edge-case scenarios both independently numbered 1-8), P1 Capture Post-Onboarding (3 scenarios, built from scratch — this page had never been scenario-organized before), P4 Journal Synthesis (11 scenarios, consolidated from ~19 previously-scattered single-screen sections), P5 Search & Discovery (3 scenarios), P6 Menu/Navigation (1 scenario — see gap below), P8 Notifications (1 scenario), P9 Account Profile (3 scenarios), P10 Today Tab (1 scenario), P11 Growth (2 scenarios, MVP/Post-MVP split — see below).

**Real recurring Figma API bug found and repeatedly worked around:** reparenting a node into a newly-created Section via `appendChild` does not reliably preserve intended local coordinates when the node's parent (or the section itself) uses auto-layout — children would silently jump to stale or stacked positions, rendering outside the section's visible bounds. Fix pattern that worked consistently: explicitly set `layoutPositioning = 'ABSOLUTE'` on the child before/after reparenting, and always verify with a real screenshot rather than trusting the returned position data alone (get_screenshot itself also showed real render-lag on a few occasions, confirmed by re-fetching).

**Growth Tab (P11) MVP/Post-MVP line clarified and locked, correcting an over-broad first pass:** Kell confirmed the Narrative feature itself (thin narrative text + "Yes, that's me"/"Not quite" confirmation) **is MVP**, along with Rhythm and Intent editing (both already documented as not touching Formation Intelligence, per T-195/PILLAR_GROWTH_STRATEGY.md). What's genuinely Post-MVP is only the three deeper Tier-2 sections — "How Your Tone Has Moved," "Who's Been Part of Your Story," "A Moment Worth Revisiting." Built the missing "Yes, that's me" confirmed-state screen (previously only "Not quite" had a dedicated resulting screen) — a quiet in-place acknowledgment (checkmark + "Confirmed" + footnote), not a new screen, matching the asymmetry that agreeing needs no correction step.

**T-195 closed this session** — the duplicate Rhythm/Intent rows Kell flagged in Preferences were removed directly during the P9 Account Profile reorganization, with the description card corrected to note Rhythm/Intent are now owned and edited in Growth, only summarized/linked from Preferences.

**Two gaps surfaced and ticketed rather than guessed through:** (1) **T-202** — "Sit with this" on the Today tab's daily verse has no designed Capture-entry screen anywhere in Figma; resolved conceptually via a published HTML artifact (opens Capture with the verse as the prompt) but not yet built for real. (2) **T-203** — P6 Menu/Navigation's tab-bar-shell-final, capture-icon-options, and 4 nav-style-variation content that existed earlier in the session was gone by the time Scenarios 1-3 were attempted; cause unknown, not traced to any specific action taken; Kell chose to mark the page Ready for Review as-is (Scenario 4, Default Tab Screens, only) rather than block on investigating.

**Consistent header-icon bug found and fixed across every pillar's default tab screen:** Today and Growth were using unstyled placeholder ellipses ("Capture Button"/"Profile Button") instead of the real `HeaderActionPill` component (Plus/Search/Profile with actual vector icons) that Entries already had correctly. Fixed on both the real source screens and their P6 clones; also added a missing tab bar to the real Today screen and fixed an inline-vs-floating pill positioning bug caused by the same auto-layout quirk noted above.

**File hygiene pass on the whole Figma file:** deleted ~6 confirmed-duplicate frames/sections across P0/P1/P4/P5/P9/P10/P11 (screens that were 1:1 text-identical to content already live elsewhere), and relabeled + reordered 17 non-pillar pages (research explorations, an abandoned photography-carousel direction, the now-superseded consolidated prototype page, a stale duplicate Post-Onboarding page) with a "🗄 ARCHIVED —" prefix, pushed to the bottom of the page list, so the file reads clean for an external reviewer without deleting anything outright.

---

**September 1, 2026 session — Pillar 6 (Formation Intelligence) system design fully locked, from architecture through a ticket-ready technical spec; 4 new tickets raised (T-197–T-200); numbering collision with Menu Bar resolved:**

**Full system-design pass on Formation Intelligence, walked input-by-input with Kell rather than sketched top-down.** Built two companion FigJam boards: the existing "System Design" board (engine flow, Narrative Confirmation, cross-pillar consumer map — all sections now wired with connectors that were previously disconnected) and a new "Inputs & Outputs Map" board, built row-by-row across 7 inputs at explicit/implicit input-engine-output granularity: Onboarding Intent (write), Capture (read), Journal entry created (two sequential LLM calls, write), Intent changed later (write, via Growth P11 not Account Profile — corrected against T-195), Journal edited after creation (confirmed no-op for MVP, not permanent), Narrative Confirmation (branching Yes/Not-quite, renamed from "Confirmation Loop," rate-limited to once a month), and Monthly full-batch reconciliation (three-condition gate: reflection tier + recent profile activity + paid tier).

**Two engines locked as separate, sequential LLM calls, not one combined call:** Theme Detection (cheap, incremental, per-journal) feeds structured `DetectedTheme` records up to the Dweller Profile Engine (the higher-order layer that writes the Narrative). Kept deliberately separate for independent debuggability, despite the extra token cost. Cadence model researched against how ChatGPT ("Dreaming") and Mem0 actually work, landing on a hybrid: cheap incremental merges per event, plus a locked monthly full-batch reconciliation that rereads everything from ground truth for a gated subset of users only — the real long-term cost driver, unlike the flat incremental side.

**Two net-new data models defined for the first time:** `DwellerProfile` (never had a concrete struct in any doc before this session) and `CorrectionRecord` (weighted the same as a capture in how much it counts toward knowing the user, but stored as its own distinct type — never labeled a capture, never a visible journal entry).

**Full technical spec written and ticket-ready:** `docs/PILLAR_6_FORMATION_INTELLIGENCE_TECHNICAL_SPEC.md` (also mirrored to a new Notion subpage under Pillar 6) formalizes all 7 rows with concrete trigger mechanisms, read/write behavior, and error handling, plus 8 explicitly flagged open items (none silently assumed) — the two biggest blockers being an undefined paid-tier structure and an imprecise "once a month" window. Thin pointer sections (not spec duplicates) added to the 5 originating pillar docs (Onboarding, Capture, Journal Creation, Settings/Account Profile, Growth) referencing the new spec.

**Numbering collision resolved:** "Pillar 6" was previously double-claimed by Formation Intelligence and Menu Bar/Navigation. T-076/T-078 (Menu Bar tickets) renamed to epic "Menu Bar / Navigation (Cross-Cutting)"; T-180's stale note corrected to reflect the resolution.

**4 new tickets raised** (T-197–T-200): T-197 (define Intent options + single-select onboarding UI), T-198 (define the qualifying paid tier for the reconciliation gate, blocking), T-199 (define the precise "once a month" window for rate limits), T-200 (the actual engineering ticket to build the DwellerProfile/CorrectionRecord models and the two-engine architecture — explicitly blocked on T-198/T-199 and the tech spec's other open items).

---

**August 31, 2026 session (second session) — Pillar 11 (Growth Tab) fully designed in Figma from scratch, superseding a stale strategy doc found mid-session; T-161 rewritten with the full narrative-tier model; 6 new decision/cleanup tickets raised:**

**Opened by re-discovering real prior work.** Kell asked to design Growth, unaware that an Aug 28 session had already locked a very different architecture than `PILLAR_GROWTH_STRATEGY.md` (Formation Overview/Emotional Themes/Prayer stats) describes — found two Aug 28 Claude Artifacts ("Your Narrative — Growth Tab", "Growth Tab — MVP vs Post-MVP") that never made it into TICKETS.csv, the same docs-drift pattern that hit P4/P5 before. Corrected course to build from that real baseline (Your Narrative + Your Plain Stats + Settings) instead of the stale strategy doc.

**Designed and locked the full Reflective-Density-gated narrative tier model (T-161), a real product mechanic, not just screens.** Two separate mechanisms: a **validity floor** (spam/junk filter — a reflection must contain genuine content to count at all, but even a single genuine word like "ouch" counts) gates eligibility; **Reflective Density (T-127)** never gates eligibility, it only modulates how rich the generated narrative can honestly be. Three tiers: **0-2 qualifying reflections** = no narrative, soft directional copy, deliberately no visible count/countdown (Kell's call, to avoid a gameable numeric target); **3-5** = a thin, honest, affirming narrative unlocks even off minimal input (the "ouch" precedent, explicitly modeled on Untold's approach); **6+** = the same narrative deepens/continues, never restarts as a new one. Regeneration is **never user-triggered** — runs automatically (presumed on app load), consistent with T-161's original "threshold-based not real-time" framing. Confirmation loop: "Yes, that's me" logs silently with zero visible UI change; "Not quite" opens a free-text feedback field feeding the next reassessment cycle, not an instant rewrite.

**Built all 7 screens in Figma** (new `P11 - Growth` page, `t5MUGEtpeFcUixobvHiYMc`): Home (Today Tab reference), Growth Tier 0/1/2, Narrative Feedback Open ("Not Quite"), Rhythm Open, Intent Open — all reusing existing components/tokens (Today's header, `TabBar v3 (3-tab) / Growth Active`, Preferences' Rhythm/Intent dropdown rows) rather than inventing new patterns. Real bugs hit and fixed along the way: duplicate loose Rhythm/Intent rows pasted directly onto the frame outside the layout system (caused the overlapping/cut-off mess Kell flagged), a tab bar rendering *behind* scrolling content instead of floating above it (z-order fix), a `Body` auto-layout frame stuck at a fixed height clipping the Rhythm/Intent dropdown panels entirely (`primaryAxisSizingMode` fix), and a text-truncation bug on the progress copy.

**Two mid-session reversals, both correctly reasoned through, not arbitrary:** (1) Settings (Rhythm/Notification link) was first built into Growth per the old strategy doc, then **removed entirely** once Kell pointed out Account Profile's header icon already gives one-tap access from every tab — no duplicate entry point needed. (2) Rhythm and Intent were then **moved the opposite direction** — out of Account Profile and into Growth entirely (not just referenced) — since they're formation-loop drivers (T-178) that thematically belong where the narrative lives; Account Profile was reflowed to drop them cleanly, no leftover gap. Also: **Prayers Offered** stat removed (Prayer is Post-MVP, showing a permanent zero read as broken) and **Journals Kept** explicitly rejected by Kell (implies journals are deletable, which they're not) — Plain Stats now shows moments-captured only.

**Offline/online model locked and written into T-161**, including a real cross-check that surfaced a genuine gap: Growth's Narrative generation is an LLM call not accounted for anywhere in T-119's token budget split. Kell's ruling — **one single unified LLM budget, no per-feature carve-outs** — so T-119 was flagged (not Today's T-185 "zero LLM calls" treatment) to add a new Narrative line item.

**6 new tickets raised** (T-191–T-196) capturing what's left as decisions/cleanup rather than open design work: T-191 (does Your Narrative satisfy/supersede T-181's "Worth Noticing" concept), T-192 (keep/drop the old Time Range Filter), T-193 (confirm validity-floor rejection is invisible to the user), T-194 (true-zero-moment Tier 0 copy), T-195 (remove the now-duplicate Rhythm/Intent rows still sitting on Preferences (MVP)), T-196 (sync `PILLAR_GROWTH_STRATEGY.md` and T-140-144 to this session's actual locked design).

**Kell's explicit sequencing for what comes after this:** Pillar 11 (this session) and Pillar 8 (prior session) close out the current design pass — **Pillars 6 and 7 are the only pillars left needing a first-round UI design.** After those two, the next phase is *not* more first-round UI — it's T-190's cross-pillar sensory pass (sound/haptic/animation), this time built as an actual **HTML prototype per pillar**, not just a written schema, done alongside a UX designer collaborator joining the project. Cost analysis per pillar comes after design is holistically complete, explicitly sequenced last.

---

**August 31, 2026 session (first session) — Notification funnel (Pillar 8) fully rebuilt and reconciled against the Aug 3 Prayer deferral; all four corrected stages (A/B/C/D) designed, confirmed, and built in Figma; Today tab icon redesigned; T-179 resolved:**

**Root problem found and fixed: the entire notification funnel was built around Prayer events that no longer exist.** Every stage in `NOTIFICATIONS_ENGINEERING_HANDOFF.md`, `NOTIFICATIONS_IMPLEMENTATION_SPECS.md`, and the canonical Notion Pillar 8 page gated on `prayer_completed`/prompt events — stale since Prayer moved to Post-MVP on Aug 3. Rebuilt the funnel around the real MVP loop (Capture → Dwelly → Journal) as four stages, relettered to match Notion's own A-G convention rather than the ad-hoc A0/A/B/C/D scheme first proposed: **A** (incomplete onboarding, off-app only — permission-gated, no channel exists for anyone who never reached Screen 6.5), **B** (captured, journal never opened — off-app + in-app on Today, the only stage with an in-app surface), **C** (dormant 14+ days — off-app only), **D** (actively dwelling — off-app only, "silence is the product," matching Notion's original spec). Notion's canonical Pillar 8 page rewritten to match; also corrected a false claim there that journal entries "only exist as AI synthesis from prayer" (contradicted by P4's actual architecture).

**In-app scope deliberately narrowed mid-session:** Kell scoped in-app nudges down to Stage B only, journal-review framing exclusively — no in-app card for Stage C (Today's own Daily Prompt already nudges capture; a second card would duplicate it) or Stage D. New **T-188** captures this rule; new **T-189** (Post-MVP) tracks weaning the Stage B in-app card off once a user shows an established self-review habit, threshold TBD.

**Today tab icon fully redesigned** through several rounds with Kell (sunrise → home outline → Instagram-style filled house → stencil outline → final rounded-corner house-with-door-cutout, matched against a real Instagram Mobbin reference and Kell's own hand-drawn spec). Real fixes caught during the process, not just taste calls: a background-box rendering seam, an SVG arc-command incompatibility (Figma vector paths don't support `A` commands, cubic beziers only), and a dot mispositioned ~1500px off-canvas from a reparenting coordinate bug. **Updated at the master-component level** (`TabBar v3 / Today Active`), so it propagated automatically to every instance file-wide, verified on Pillar 6's actual production Today screen.

**Wayfinding dot redesigned twice more after initial build:** first corrected from red to gold (semantic mismatch — red reads as error/urgent, wrong tone for an invitational app) and from a floating circle to a proper corner badge; then given a live animated pulse (soft expanding-ring, 2.2s loop, stops when the user resolves the nudge rather than on a timer) after Kell asked whether a static dot alone was enough signal.

**New sensory design work:** built a full per-stage sound/haptic/animation schema for T-187 using real iOS API vocabulary (`UIImpactFeedbackGenerator(.soft)`, interruption levels, etc.) rather than vague adjectives — finding that Stage B's in-app card is the only place with anything custom to design; A/C/D's off-app pushes just inherit standard OS behavior. New **T-190** (raised at Kell's explicit request) generalizes this same rigor to a first-pass sensory schema for every UI pillar, not just notifications — the confirmed next-session objective, to be handed to a UX designer collaborator after this first pass.

**T-179 resolved** — all nine sub-questions (offline sync, permission revocation, multi-device, Quiet Days mechanics, Rhythm empty-state, etc.) answered and locked; Quiet Days itself deferred to Post-MVP entirely rather than simplified.

---

**August 28, 2026 session — Pillar 9 (Account Profile) Security & Privacy, Preferences, and Support & Feedback subpages designed and built in Figma (MVP + Post-MVP parity), plus a device SSH setup and repo sync at session start; T-179 raised:**

**Session opened with device setup, not design work.** This was a new device without GitHub SSH access — generated a fresh `ed25519` keypair, walked Kell through adding the public key to GitHub, verified `ssh -T git@github.com` authenticated, then found the device 2 commits ahead of `origin/main` and pushed them. Routine but worth logging since it blocked everything else until resolved.

**Security & Privacy subpage built — MVP and Post-MVP as two separate pages, not one page with inline Post-MVP tags.** Kell's explicit correction: MVP page = Password + Encryption only; Post-MVP page = the same two carried over, plus Data Export and Session Management (with a sample device list, current-device tag, per-device Sign Out). Change Password (3 states: default/error/success) and an in-app Encryption "Learn More" screen were built as their own Artifacts first, per Kell's call that off-platform links are unacceptable — the encryption explanation needed to live natively in the app, not link out to a website. All then built in Figma matching the confirmed base Account Profile screen's exact tokens (colors, Instrument Serif/Inter, card system) pulled live via `get_design_context`.

**Preferences subpage built — Rhythm, Intent, Notifications, Theme (Post-MVP only).** Kell added Intent to the original Rhythm-only plan (shared field with Account Profile). Initial design used full-screen radio-card selection screens (mirroring Onboarding's rhythm-selection/intent-selection pattern) — **Kell rejected this**, requesting inline dropdown pickers instead (tap the value row, a panel expands in place, no screen navigation). Rebuilt accordingly; all four dropdown/expanded states (Rhythm open, Intent open) built in both MVP and Post-MVP. Notifications got its own full "Manage Preferences" screen (3 modes — Default/Sparse/Off — + Sabbath/Quiet Days toggle with per-day selection), also mirrored MVP/Post-MVP.

**Support & Feedback subpage built — Send Feedback, Report a Bug, Contact Support, FAQ & Help Center (Post-MVP only).** Contact Support uses a native `mailto:` (opens Mail app, stays on-device) rather than a web contact form, consistent with the no-off-platform-links rule. FAQ is the one deliberately Post-MVP-only feature — disabled/tagged in MVP, fully enabled with its own native in-app browse screen (search bar + Q&A cards) in Post-MVP.

**MVP/Post-MVP parity rule locked and enforced structurally, not just visually:** Kell's explicit standing rule — whatever exists in MVP must also exist in Post-MVP (copied forward), with Post-MVP only ever *adding* genuinely new features, never omitting something MVP has. Two Figma organization passes were needed to actually satisfy this: (1) built two named Figma Sections ("MVP" / "Post-MVP") to visually group every screen — first attempt had section bounding boxes badly mismatched to their actual child positions (a coordinate-space bug: child x/y become relative to the section once parented, not page-absolute, and the first pass didn't account for that), fixed by recomputing every frame's position in a clean explicit grid per section; (2) a later pass found "Security & Privacy - Post-MVP" almost exactly overlapping "Preferences (Post-MVP)" (same relative x), making it invisible/hidden behind — root cause was ad-hoc repositioning across several incremental `use_figma` calls without re-verifying the full grid — fixed the same way, by relaying out both sections' full children lists in one deliberate pass. Kell also clarified that **duplicate "shared" screens appearing in the Post-MVP section (Account Profile base, Change Password, Encryption) are intentional**, not a bug — he wants the full end-to-end flow visible in both sections even where they're identical; these were renamed with a "(Base MVP)" suffix to signal they're carried over unchanged, not new work. Final state: **MVP = 12 screens, Post-MVP = 13 screens** (FAQ is the only screen Post-MVP has that MVP doesn't).

**New ticket T-179 raised and built out over several turns.** Kell caught that the Notifications "Manage Preferences" screen (a simple 3-mode picker) was designed without checking it against Pillar 8's actual locked notification architecture (T-084/T-085/T-086's 7-stage A-G funnel with per-stage cadence caps and push+in-app suppression parity) — raised as a cross-pillar reconciliation ticket, not a duplicate of T-086. Kell then walked through offline/online considerations (preference writes should follow the same offline-first local-write pattern as moments, but Pillar 8's server-side stage engine creates a sync-lag question) and five more system-oriented scenarios (OS-level notification permission revocation, multi-device conflicts, save failure handling, timezone effects on Quiet Days, first-time/no-prior-value state) — all folded into T-179 as one combined design pass, since Kell judged none of them were separate design questions warranting their own tickets.

**Not yet done (explicit carry-forward):** **Legal & About** — the 4th and last Settings subpage — has not been touched at all. **Account Deletion flow (T-095)** — Apple's required in-app self-service deletion — has no design yet. **T-178** (Rhythm/Intent as formation-loop behavioral drivers) and **T-179** (Notifications/Pillar-8 reconciliation) both remain open design questions, tickets only — the actual design/systems-thinking work for either has not started.

---

**August 27, 2026 session (third same-day session) — P4/P5 docs-sync debt closed out, T-159 resolved, Pillar 9 (Account Profile) base screen designed via Artifact-first workflow, then built in Figma:**

Cleared all 3 overdue catch-up items flagged at session start: (1) `docs/P4_SUMMARY.html` and the Notion P4 page fully synced against every Aug 26-27 decision (pending flame/badge treatment, no manual retry, offline indicators dropped from Edit/View, hero-photo layout, Mood Picker bottom-sheet + inline "+ Add mood" pill, View Original's staggered chat clustering). (2) **T-159 resolved** — Kell confirmed a two-mechanism background retry design for Pending entries: a periodic `BGTaskScheduler`/`BGAppRefreshTask` sweep (primary, works even if the app stays closed) plus a piggyback sweep on `SyncManager`'s new-capture sync (fallback/fast-path); no manual Retry UI, no dead-end state, ever. Ticket retitled "Build Background Retry Sweep for Pending Synthesis." (3) The Notion "P5 User Scenarios & Acceptance Criteria" page — found to already exist (stale, from the old July 10 two-screen/Prayed-Object-filter model), not missing as prior MEMORY entries assumed — fully rewritten to match the current locked design, and `docs/PILLAR_5_SEARCH_STRATEGY.md` rewritten to match.

**Pillar 9 (Account Profile) base screen designed and built, following an explicit process correction from Kell: Claude Artifacts first, Figma only after Kell confirms — never design-first.** Iterated through several real architecture changes via artifact before locking: (1) the gear-icon-on-Growth-tab-only entry point (locked July 11, 2026) is **superseded** — Account Profile is now reached via a persistent profile icon in the header pill on **every** tab, alongside the persistent Capture "+" button; (2) the destination is titled **"Account Profile,"** not "Settings" — identity fields (avatar, name, email, Rhythm, Intent, Dweller Since, Subscription) render directly on this page, no tap required, with **Settings** collapsed into one grouped card below it (Security & Privacy / Preferences / Support & Feedback / Legal & About, one bordered card with divider lines between rows, icon + title only, no subtext); (3) transition mechanics: tapping the profile icon **pushes** Account Profile in from the right (NavigationStack), consistent with the already-locked rule from Pillar 5 (destinations push, creation slides up as a modal) — not a modal/sheet as the July 11 design originally specified; (4) the original "Name" field row was dropped from the field-group (redundant with the identity card above it) and replaced with **Rhythm**; (5) two situational/conditional cards — a new **"Getting to Know You"** rotating-question concept, and the existing **Intent Check** prompt ("Is Dwellable helping you [intent]?") — were explored, then both explicitly **parked out of the base screen** per Kell's call to keep the base screen to just identity + Settings; the Intent Check additionally gets a new locked timing rule (fires after ~2 weeks of use / after the free trial ends, not from first login) that still needs to be written into the P9 doc/tickets next session.

**Built in Figma:** new page **"P9 - Account Profile"** created (file `t5MUGEtpeFcUixobvHiYMc` — the file's Figma-desktop bridge was only surfacing one page this session, `📱 As-Built (Phase 1) — Reference`; Kell confirmed no P9 page existed yet and to create one fresh rather than chase down why other pages weren't visible). Base screen built matching the confirmed artifact, using the existing `Dwellable/Colors` variable collection (dark near-black `background/primary`, `accent/gold`, etc. — not the Onboarding cream/gold system). **Two real bugs hit and fixed during the build, both worth remembering for future Figma icon work in this file:** (1) calling `.resize()` on a vector *after* setting `vectorPaths` stretches/distorts the tight bounding box Figma auto-computes around the path data — small glyphs (clock hands, chevrons) got badly warped; fixed by never resizing a vector post-`vectorPaths`, letting the natural bbox stand. (2) A blanket `findAll(n => n.type === 'VECTOR' && n.name === 'Vector')` fix-up script matched far more nodes than intended, since every vector created via `figma.createVector()` defaults to the name "Vector" — this silently overwrote several icons' paths with chevron data. Fixed by a full clean rebuild of the Settings Card's 4 rows with every vector explicitly named (`ShieldGlyph`, `ClockHands`, `ChatTail`, `InfoStem`, `RowChevron`, etc.) to prevent recurrence.

**New ticket T-178 raised** (not yet designed): Kell flagged that Rhythm and Intent (both editable from the new Account Profile screen) are meant to be behavioral drivers of the whole formation loop, not static fields — e.g., a "deepen intimacy with God" intent should imply/expect a certain capture rhythm — but nothing in the system currently connects them, and no design exists yet for what happens when a user changes either post-onboarding. Explicitly flagged by Kell as a systems-design question requiring its own design pass before any engineering ticket can be written.

---

**August 27, 2026 session (continued, second same-day session) — P4 confirmed fully designed and closed out, its Figma page deduped/reorganized, and Pillar 5 (Search & Discovery) designed end-to-end from scratch (6 scenarios, ~14 screens, all built in Figma); engineering tickets rewritten to match, none newly built (pure design):**

**P4 confirmed complete before moving on.** Walked every one of the 11 originally-scoped P4 scenarios against what was actually built/decided: all are now either ✅ built or deliberately descoped (Synthesis-fails-permanently-fallback and both offline-mid-edit scenarios were explicitly removed in the immediately-prior session entry, not abandoned by oversight). Rewrote the stale engineering tickets to match the current architecture: **T-151-153/156/158 updated** (no Object field, no Prayer dependency, starred boolean, Edit Moods bottom-sheet naming), **T-154 (Object UI) and T-157 (Resonance-Gated Prayer) marked ⚪ Dropped** with reasoning kept rather than deleted, **T-155 rewritten** from the old "Entry/Dwelling Place Two-Tab UI" to "Journal Detail Screen (View+Edit)" and folded in a new locked decision (✕ with unsaved edits shows a confirm-discard modal, not silent discard), **T-159 flagged** (not silently rewritten) — its "permanent fallback" premise contradicts the prior session's "Pending never permanently fails" decision; Kell's call needed on whether Pending can ever truly dead-end. Also renamed node `1399:48` in Figma from unlabeled to "EDIT ENTRY — KEYBOARD ACTIVE STATE" after confirming it was a legitimate distinct state (toolbar riding above an active keyboard), not clutter.

**P4's Figma page deduped, then reorganized into rows** (two separate passes, both delegated to background agents and manually verified after). Kell had already deleted a wholesale duplicate of the entire scenario set himself; the remaining internal clutter (11 copies of "Scenario 1 — View", 12 of "Ellipsis Menu Open", 9 of "Edit Entry — Neutral State", plus 2 real auto-nesting bugs where sections had gotten nested inside Hero Photo containers) was reduced to exactly one canonical copy per concept — 37 duplicate nodes deleted total, verified by re-scanning that every previously-duplicated name now appears once. A second pass then reorganized the deduped content from scattered ad-hoc positions into 10 clean labeled rows per Kell's explicit complaint that the layout "should be much more organized."

**Pillar 5 (Search & Discovery) designed from scratch, six scenarios, all confirmed via Claude Artifact before Figma per the established one-scenario-at-a-time pattern.** Corrected the pillar-numbering confusion first (multiple docs disagreed on whether Search was Pillar 5 or 6 — `TICKETS.csv`'s existing ticket numbering settled it as 5) and identified `docs/PILLAR_5_SEARCH_STRATEGY.md` as canonical among 3 near-duplicate strategy docs. **Locked filter-model correction:** dropped the **Prayed** filter (Prayer has zero P4 integration) and the **Object** filter (field doesn't exist in P4's model) — Search filters are now Mood (single-select) + free-text only, both **T-128 and a new backlog ticket T-177** (Post-MVP full-transcript search, explicitly deferred by Kell) capture this.

**Scenario 1 — Calendar + Entries List:** Untold-style month calendar + entry list, Pending entries shown only via a small pulsing "Pending" tag (Approved entries carry no status label at all — absence *is* the Approved state, per Kell's explicit correction). Iterated the header several times per Kell's direct feedback: tab bar corrected from an invented 4-tab set down to the real locked 3-tab set (Today/Entries/Growth, Capture is not a tab), then a floating "+" was added, then merged into one pill with search, then a profile icon was added to the same pill after Kell referenced the Journey app, then the plus icon was corrected to match the real locked Pillar 6 `HeaderPlusButton` component exactly (thin gold stroke, not a bold glyph) instead of an invented style. Also fixed a real UX bug Kell caught from a screenshot: the tab bar was a solid opaque block cutting off list content — corrected to a translucent/blurred floating pill (iOS's actual native tab-bar treatment) with the list scrolling fully underneath it.

**Transition mechanics locked via an interactive HTML demo before touching Figma:** tapping an entry or the magnifying glass both **push** a new screen in from the right (NavigationStack pattern, back-arrow to return) since they're destinations you navigate to; tapping "+" **slides Capture up** from the bottom as a modal (✕ to dismiss) since it's creating something new — deliberately different gestures for different intents. A mid-transition storyboard frame (list dimmed/shifted left, View sliding in with a leading-edge shadow) was also built directly in Figma as a documentation artifact, matching the storyboard convention used for the Capture slide-up work in earlier sessions.

**Scenario 2 — Search, five states:** idle (Recent Searches with per-item and "Clear all" removal, both instant with no confirmation modal since search history isn't user content), real-time typed results (matching text highlighted in gold, Mood filter combined via AND logic), no-results (reassuring empty state, not blank space), one-recent-search-removed, and all-recent-cleared (a quiet inline message, deliberately *not* a big illustrated empty state — kept low-key since nothing of value was lost). **Locked:** Pending entries are excluded from search entirely — not indexed, never appear in results, still reachable by scrolling the list — and Search runs fully offline against already-synced local data (no special offline state needed, consistent with P4's Edit/View offline-first precedent, which was also confirmed to extend to Delete).

**Delete-reflected-in-list scenario built as a full 4-step storyboard** (List → View → Delete confirmation → List again, entry and its calendar dot both gone), deliberately reusing Pillar 4's already-locked View/⋯-menu/delete-confirmation screens rather than inventing new ones. This surfaced two real decisions: **(1) new architecture call from Kell** — Recently Deleted entries are now browsed/restored from a new section in **Settings**, not from within the Entries list itself, to keep the list showing only active entries (**new ticket T-176**, Pillar 9). **(2) A real copy discrepancy** between the locked docs language and what was actually sitting in the Figma popover — resolved by locking new copy that names the location explicitly: *"This entry will move to Recently Deleted in Settings and be permanently removed after 30 days. You can restore it before then."* Updated in all three places (`TICKETS.csv` T-158, `P4_SUMMARY.html`, and both the canonical P4 popover and its P5 clone in Figma) — hit and fixed one real Figma auto-layout bug along the way (making the button row `ABSOLUTE` to reposition it silently excluded it from the parent's hug-height calculation, clipping the buttons entirely; fixed by leaving it in normal auto-layout flow instead).

**Not yet done (explicit carry-forward):** ~~T-159's Kell-decision is still open~~ — **RESOLVED in the next session (Aug 27, 2026, later):** Pending never dead-ends; retries continue silently and indefinitely via two background mechanisms — a periodic `BGTaskScheduler`/`BGAppRefreshTask` sweep (primary, works even if the app stays closed) and a piggyback sweep on `SyncManager`'s new-capture sync (fallback/fast-path). No manual Retry UI. T-159 retitled to "Build Background Retry Sweep for Pending Synthesis" and rewritten in `TICKETS.csv`; see `docs/P4_SUMMARY.html` and the Notion P4 page's Scenario 4 for full rationale. The Notion "P5 User Scenarios & Acceptance Criteria" page does not exist yet and should be created to match the pattern already established for P4. `PILLAR_5_SEARCH_STRATEGY.md` itself still needs a doc-sync pass to reflect everything locked this session (filter model correction, Pending exclusion, the two new tickets). T-176 (Recently Deleted in Settings) needs its own design pass — nothing beyond the ticket exists yet.

---

**August 27, 2026 session — P4 Pending-state scenario set finished, offline behavior fully re-scoped (Save works offline, no manual Retry, no offline indicator needed in Edit/View), and the last 4 outstanding artifacts (Add Photo, Add Camera, Voice Transcription, Pending View Original) built in Figma; no engineering ticket status changes (pure design):**

Opened by auditing every Edit-state screen in Figma for a toolbar regression (18 screens had lost 2 of their 3 toolbar icons somewhere in prior sessions' cloning — root-caused to a `figma.createNodeFromSvg()` wrapper-frame quirk, not caught earlier because screenshots at small scale hid it) — fixed all 18 in one pass.

**Pending state scenario set built from scratch, then corrected against real feedback:** Discussed and locked the Pending scenario list with Kell (base/happy-path, Star disabled until synthesis completes, ⋯ menu with Edit Entry shown-but-disabled + View Original + Delete Entry, delete confirmation reusing the Approved popover). Built all of it in Figma, iterated through two real bugs Kell caught directly from screenshots — a placeholder teardrop vector standing in for the flame logo (fixed by pulling the actual PNG asset from the P0 Onboarding page's `welcome-screen` node and embedding it via `figma.createImage()`, not redrawing it), and every ⋯ menu row centered instead of left-aligned (root cause: `primaryAxisAlignItems: 'CENTER'` set on the row auto-layout at creation — most visible on short labels like "Edit Entry"/"Retry", easy to miss on longer ones like "View Original"). Also caught and fixed a copy-consistency gap: two Pending screens were missing the reassurance line under the flame entirely.

**Retries-Exhausted branch designed, then removed same session.** Originally built a "Retries Exhausted" state with a manual Retry action (first as an inline button, then per Kell's request moved into the ⋯ menu, styled gold between View Original and Delete). After walking through it, Kell reasoned that if sync just retries quietly and indefinitely in the background, there's no real "exhausted" state to design for — removed both Figma screens (`1441:137`, `1441:175`) and the whole branch from scope.

**Offline behavior re-scoped down to almost nothing.** Built a 3-screen "Editing Offline" artifact (offline pill by the date, Save-while-offline queued state, silent reconnect), then Kell pressure-tested whether it was even needed: walked the actual dependency list for the Edit screen (typing, mood tags, photo library, camera, voice transcription via on-device Speech Framework, and Save itself via the app's existing offline-first local-write architecture) and found **nothing in Edit or View actually depends on connectivity** — Save always writes locally first regardless of network state. Conclusion: drop the offline indicator from both Edit and View entirely; Pending remains the only screen where connectivity is meaningfully surfaced, since synthesis genuinely requires it. A related product note surfaced along the way: future Formation-Intelligence/Rich-Context work should reference the raw transcript, not user-edited journal text, when building a user's context history — noted for later, not actioned this session.

**Closed out the last 4 outstanding artifacts, all built in Figma:** Add Photo (native-style library picker bottom sheet), Add Camera Photo (full-screen native viewfinder takeover — built from scratch, not cloned, since it's a true takeover not an overlay), Voice Transcription (toolbar replaced by a listening panel with waveform/timer/Cancel-Done), and Pending View Original (clone of the Approved transcript screen with a footer note appropriate to a not-yet-synthesized entry: "This conversation is being formed into your journal entry," with the same accent pulse-dot language used elsewhere in Pending). Minor build bugs hit and fixed along the way: a stray duplicate second body paragraph left over from cloning (same root cause hit multiple times this session — always check for a stale sibling text node after cloning a multi-paragraph body), Cancel-text clipping on the Add Photo sheet header (padding too tight against the phone's rounded edge), and a stretched/misaligned thumbnail+flip control row on the camera screen (`layoutSizingHorizontal: 'FILL'` applied to fixed-size elements sharing a row with a much larger sibling).

**View Original chat thread redesigned from stacked bubbles to a real staggered text-thread layout**, per Kell's direct feedback that it read as uniform cards, not a conversation. Consecutive messages from the same sender now cluster tightly (4px gap, only the last bubble in a cluster gets the rounded "tail" corner), switching senders gets a real 16px gap, and a timestamp shows once per cluster instead of per message. Applied to both the Approved and Pending View Original screens, in both the Artifacts and Figma — required splitting a few of the longer single messages into multiple shorter consecutive bubbles from the same sender to actually demonstrate the clustering effect.

**New architecture requirement locked and logged against T-120 (Dwelly Agent conversational loop, not yet built):** Dwelly must wait for the user to actually finish typing/speaking before generating a response — cannot treat the first message or a brief pause as the end of the user's turn. Some users are "multi-texters" who send several short messages in quick succession to process a thought rather than composing one long message; the conversational loop needs a debounce/composing-detection window, not a naive single-message trigger, so Dwelly never cuts someone off mid-thought.

**Not yet done (explicit carry-forward):** the Notion "P4 User Scenarios & Acceptance Criteria" Design Status table has not been touched since the very first P4 session two days ago — it does not reflect any of the ~20 scenarios built since. `P4_SUMMARY.html` is similarly stale against several decisions locked this session and last (Pending badge/flame treatment, no manual Retry anywhere, offline indicator dropped from Edit/View, hero-photo layout, Edit Moods bottom sheet, toolbar dropping the Tag icon in favor of an inline "+ Add mood" pill, staggered View Original thread layout). The Figma file itself has accumulated real clutter from concurrent editing across sessions — multiple duplicate copies of several sections at different node IDs, and at least one section that got auto-nested inside another and had to be detached mid-session.

---

**August 26, 2026 session (continued, later same day) — P4 Journal Detail scenario inventory driven to completion for the "online" (synced) state — 13 scenarios walked, reviewed, and built in Figma; no engineering ticket status changes (pure design):**

Picked up directly from the earlier same-day entry below (Pending/View/Star/Ellipsis/Delete already done). Kell walked a numbered list of every scenario needed for the online state; each was iterated as a Claude Artifact first, then built into Figma on the `P4-Journal Synthesis` page once approved. **Delivered:** Delete Confirmation redesigned from a large centered modal into a subtle popover anchored in the ⋯ menu's own slot (3 placement options explored, Option A chosen); Edit Entry happy path (neutral + keyboard-focus states, Undo/Save tracking dirty-state not "something happened"); Add Photo / Add Camera / Voice Transcription entry points; hero-photo layout (full-bleed image above the title, title left-aligned, moods between title and body — applied to both View and Edit); Edit Moods bottom-sheet picker (locked over 3 lighter alternatives — popover swap, in-menu step, inline pill); toolbar's Tag icon removed in favor of an inline "+ Add mood" pill in the mood row itself; Add Custom Mood (typing + resolved states, capped at 3, outlined pill styling to mark user-made vs. preset); No Tags states for View (row omitted entirely) and Edit (bare add-pill); six Minimum-Content-to-Save states (1-char body, 1-char title, mood-only, title-only, photo-only, and fully-empty-with-disabled-Save); View Original Conversation (read-only Dwelly transcript, chat-bubble UI, reached from ⋯ menu — closes out the last carry-forward item from the prior entry).

**Key decisions locked:** Save/Undo icons are dirty-state indicators (differ-from-last-saved), not "anything happened" indicators — confirmed with Kell for the Undo mechanic. An entry can never be saved fully empty (Save disables before that state is reachable), so no dedicated "empty entry" screen was needed. Photo/camera capture defers to native iOS pickers — no custom UI — only the *entry points* and the *resting hero-photo state* needed designing. Delete-in-list is explicitly **not** a P4 concern — flagged as a List/Search pillar dependency.

**Real bugs hit and fixed:** a new section landed with bounds overlapping one of Kell's own in-progress sections and Figma auto-nested it as a child, producing a garbled multi-panel screenshot that looked like a real layout bug until traced — fixed by detaching back to a top-level page child and relocating to open canvas space. `figma.createAutoLayout()`'s default opaque-white fill bit again on a button row (the *n*th time this exact bug has hit this file — see repeated entries below). A `findOne()` grabbed the wrong same-named text node (the entry's own mood-row "Grateful" vs. the sheet grid's "Grateful") when not scoped tightly enough — no visible fix needed once diagnosed, but a reminder to scope `findOne`/`query` to the narrowest containing frame, not the whole clone, when duplicate labels exist across a screen.

**Environment note:** the Claude Design canvas (multi-artboard live editor) could not be used this session — `node`/`bun` are unavailable in this environment, so `seed-canvas.mjs` cannot run. All exploratory options and final designs were delivered as standard published Artifacts (single HTML page) instead; functionally equivalent for review purposes, just not click-to-edit in the browser.

**Not yet done (carry-forward):** the pending/offline states (queued edits while offline, synthesis-fails fallback, reconnect-mid-edit) were explicitly deferred — Kell said this session covered "most of it so far for online" and the pending/offline branch is next. Also open: whether Undo itself needs its own dedicated screen beyond the icon-state pattern already built (current read: no, it's covered by existing Neutral/Active icon states — confirm this holds once pending/offline scenarios surface any edge cases).

---

**August 26, 2026 session — Pillar 4 (Journal Detail) scenario map reset from scratch, Notion doc fully rewritten, real Figma screens built one at a time with Claude Artifact review each; no engineering ticket status changes (pure design):**

**Docs reset first, per Kell's explicit instruction ("update the Notion pages... then we can start on the artifacts").** Discovered the Notion "P4 User Scenarios & Acceptance Criteria" page was still dated July 10, 2026 and built entirely around a superseded architecture (Dwelling Place/Entry tab split, Pray/Skip confirmation routing into Pillar 3, resonance-gated prayer embedding, a separate Object field) — none of which survived the Aug 25 P4 redesign. Fully rewrote that Notion page against the current model (single Journal Detail screen, Prayer Post-MVP, one universal Pending state) and against this session's newly locked header/menu decisions. `docs/P4_SUMMARY.html` updated to match: `status` renamed `"ready"` → `"approved"`, added `starred: boolean`, added 4 new Locked Decision entries for the View header (Star + ⋯ menu), Edit header/toolbar, View Original's new menu location, and corrected soft-delete confirmation copy.

**Final locked scenario map:** Two statuses — **Pending** (view-only, zero edit affordance, ⋯ menu present with only View Original + Delete Entry, Edit Entry absent entirely) and **Approved** (renamed from "Ready" — full view/edit/star/delete). No Approved→Pending transition ever exists. **View screen:** icon-only circular back button (no "Entries" label), Star icon (☆ outlined / ★ gold-filled when starred), **⋯** overflow opening a popup menu (Edit Entry / View Original / Delete Entry, delete styled red/destructive) — no in-body "View original capture" link anymore, and no photo-add prompt shown in View (nothing invites an action on a read-only screen). **Edit screen:** ✕ / centered "Edit Entry" title / Undo icon / gold checkmark-Save (saves and returns to View in one action), floating toolbar above the keyboard with exactly Photo · Camera · Mic (transcribe) · Tag — no Send icon. Date visible but never editable.

**Reworked the whole Figma build process this session** after Kell caught real problems with the prior (same-day, earlier) batch-built version: onboarding-only content that didn't belong on a journal page, unclear jargon labeling, a header/toolbar pattern that didn't match his own hand-built Figma reference frames (ELLIPSIS MENU, EDIT — FILLED STATE, DELETE ENTRY — CONFIRMATION). Kell wiped the `P4-Journal Synthesis` Figma page himself and asked to rebuild **one screen at a time**, each screenshot-verified at true iPhone size (393×852pt) and published as its own Claude Artifact for review before starting the next. Delivered this session: Pending (with the ⋯ menu resolution baked in), Scenario 1 View (icon-only back button, no photo-add prompt in view mode — both fixed after Kell's direct feedback), Starred state (gold-filled star), the Ellipsis Menu popup, and the Delete Confirmation modal (soft-delete copy, 30-day recovery). Added a **Design Status tracking table** to the Notion page, split into Pending-lineage vs. Non-Pending(Approved) scenarios, so progress is checkable at a glance — 3 of 11 scenarios marked ✅ Done this session (Pending, Star/Unstar, Soft Delete), plus the View happy path.

**Real bugs hit and fixed:** the ⋯ menu popup and delete-confirmation card both rendered as a near-invisible 10px-tall sliver — root cause was calling `.resize(w, 10)` on an auto-layout frame before setting `primaryAxisSizingMode`, which locked the frame at FIXED height instead of letting it hug its actual content; fixed by setting `primaryAxisSizingMode='AUTO'` after resize. A duplicated ✕/nav-row also slipped into an early "Editing — Offline" build from a leftover unused helper-function call; caught via screenshot, rebuilt clean.

**Not yet done (explicit carry-forward):** 7 of 11 scenarios still need their own Figma screen + Artifact — Edit (happy path), Synthesis Fails (fallback), View Original Capture, Add/Remove Photo, Edit Moods via Tag, Approved-edited-then-offline, and Connection-restored-mid-edit. Kell asked to pick this up next session, one screen at a time, same review-before-proceeding pattern.

---

**August 25, 2026 session (continued, later same day) — Onboarding/Capture loop-signal system built across Figma + HTML prototype; Pillar 4 (Journal Detail) redesigned from a stale July 9 doc; no engineering ticket status changes (pure design/prototype):**

**Thread 1 — Loop signal (bar + flame), P0 Onboarding + P1 Capture Onboarding, real Figma pages, not just playground:** After iterating through several concepts in an HTML playground (circular ring — rejected, moon-crescent bug found and fixed, several "beyond the circle" alternatives explored), locked on a **flame-fill icon** carrying the per-loop signal while the **horizontal bar tells one static truth**: holds flat at 92% while a loop is in progress, jumps to 100% only when the loop actually finishes, regardless of cause. Built into the real (non-playground) Figma pages: **P0 — Onboarding** — removed the dead 🙏 prayer-hands reward icon from all 11 screens that had it, widened the bar on all 15 bar-bearing screens (294px→324px, fill % preserved) to reclaim the freed space, fixed back-arrow/bar vertical misalignment on the 7 account-creation/intent-selection edge-case screens (also replaced a dead *hidden* 12-dot progress component with the real gold bar there — found via Kell's "where's that pre-bar indicator" question). **P1 — Capture Onboarding** — audited and fixed all 37 full-screen frames (not just the original r1/r2/r3 set): added bar+flame to `B-guided-prompt-home` ×7, `keyboard-active-state` ×2, `dwelly-prompt-conversation` ×4, `dwelly-conversation-flow`, `dwelly-conversation-scrolled`, `CTA-5 — Rich Journal Preview` ×4 (100%/full-glow, matches journal-ready state), 3 early-exit variants, 2 `Header Done` variants (caught and corrected 2 that were defaulted to the wrong fill % — both show the completed journal card, should read 100%, not mid-loop), and `journal-cta-outlined-pill`. **Removed 101 total instances of a decorative centered flame/"Dwelly" wordmark logo** page-wide (51 `LogoHeader` frames + 50 orphaned "Dwelly" caption text nodes) that were overlapping the new bar on several screens — tightened header spacing after removal. Real bugs hit and fixed along the way: a stray leftover duplicate "canvas" sub-frame from an earlier failed script was rendering on top of real edits (screenshots kept showing stale content despite correct `get_metadata`); Figma auto-layout silently overriding manual x/y on new children (`layoutPositioning='ABSOLUTE'` fix, hit twice). **Mirrored into the standalone HTML prototype** (`docs/Dwellable_Full_Prototype.html`, canonical Artifact `054b3364…`): `continuityBar()` rewritten to hold at 92%/jump to 100% instead of climbing 85→100%, flame added reusing the file's own existing `ICONS.flame` asset — deliberately repurposing existing code/CSS per Kell's explicit instruction, not a rebuild. Added a soft spring-overshoot bounce (`cubic-bezier(0.34,1.56,0.64,1)`) to both the bar-fill and flame-fill transitions per Kell's "feels static" feedback, plus a scale-pop on the flame at completion. Also fixed the welcome-screen wordmark casing (lowercase "dwelly" → "Dwelly", matching last session's locked decision) while auditing the prototype.

**Thread 2 — Pillar 4 (Journal Detail) redesigned from scratch against a stale doc:** `docs/P4_SUMMARY.html` was last touched July 9 and had drifted badly — it still specified a confirmation-screen-into-guided-prayer flow (Prayer is now Post-MVP), a two-tab Entry/Dwelling-Place architecture, zero-knowledge/full-E2E encryption language (superseded July 22 by Pillar 2's actual server-side model), and old pillar numbering. Walked every conflict point-by-point with Kell and locked: **no Dwelling Place tab** — single Journal Detail screen, reached via the Entries list or "Open" right after capture; **no confirmation screen / no prayer routing**; **corrected encryption language**; **one universal pending state** for *any* reason synthesis isn't done yet (offline, LLM provider outage, exhausted API budget — no special-casing per cause, no permanent raw-transcript fallback ever, bounded retries with backoff, manual "Retry" only if exhausted). Rewrote the doc's flow steps, artifact structure, tool cards, locked decisions, tentative/TBD, open questions, success metrics, and integration-points table to match. Then designed the actual screen in HTML mockups (published as Artifacts, not yet in Figma): **4 states** — Ready, Edit (online, same screen, fields unlock in place, header swaps to ✕/Save — no modal), Offline (Ready + one small persistent status row, everything stays usable, edits queue locally), Pending (title=date, transcript fills the body slot labeled "In your words" in italics so it can't be mistaken for finished synthesis, cause-agnostic status copy). Design grounded in real reference via the Mobbin MCP (Apple Journal, How We Feel, ABY Journal, MD Vinyl, Docusign) rather than free-handed. Explored a Journal/Transcript segmented toggle (borrowed from pillowtalk on Mobbin) as a way to signal processing state through the toggle itself; Kell simplified it away in favor of the merged body-slot approach. Also designed the **edit-surface pieces**: mood picker with an inline "+ Custom" tag entry (custom tag counts toward the same 2-3 mood cap, not unlimited), photo add flow (native iOS action sheet only, no custom picker), and **View Original Capture** (confirmed as a read-only re-render of the same stored transcript data already required for synthesis — not AI-generated, not a screenshot/image — cost is close to zero since the data and the chat-bubble component both already have to exist elsewhere). All locked decisions folded back into `P4_SUMMARY.html`.

**Also this session:** Confirmed Pillar 2 (Security) already has a full locked system design (`docs/PILLAR_2_SECURITY_STRATEGY.md`, July 22) and is further along than "designed" — T-062 is code-complete (Supabase Edge Functions + migration + `SupabaseAPIClient.swift` wired), just awaiting Kell to run the deploy steps. No open design work there. Confirmed Pillar 3 (Prayer) stays Post-MVP, no changes.

**Not yet done (explicit carry-forward, Kell's own words at session close):** all of this session's Pillar 4 states (error, offline, online, pending — "everything you've created in the prototype here") exist only as HTML mockups/Artifacts. **None of it is built in Figma yet.** Next session opens with moving this into real Figma designs, same as Thread 1's loop-signal work was moved from HTML playground into the real Figma pages this session.

---

**August 24, 2026 session — Dwelly rename completed in the canonical capture artifact, real flame logo asset located, three-prototype merge scripted but NOT yet built/verified; no engineering ticket status changes (pure design/prototype):** Three threads. **(1) Root-caused the prior session's mistaken rebuild:** the earlier session rebuilt the P1 onboarding-capture flow from scratch instead of reusing the existing canonical Artifact — cause was an auto-compaction that dropped the artifact from the carried-forward record, so the next turn acted on an incomplete history and never checked the artifact list first. Lesson logged: on resume, check the artifact list before assuming a flow doesn't already exist. **(2) "Dwellable" → "Dwelly" finished in the canonical capture-flow Artifact** (`https://claude.ai/code/artifact/bf748e9b-1737-435d-838d-f3bca8f689fb`): the 5 residual "Dwellable" mentions (studio-eyebrow, sa-title, sa-body, mdm-body, one comment) changed to "Dwelly"; the tagline **"Steward every moment with God" was deliberately left unchanged**. Published to the same URL (stripped the frame-runtime wrapper that WebFetch had baked into line 1 before republishing so it wouldn't compound). Working file: `tool-results/dwelly_capture_flow.html`. **(3) Located the real new flame logo** Kell said was "off": it's the base64 PNG embedded in `docs/P0_NavChrome_Prototype.html`'s s01 welcome screen (166×120, outlined gold, S-curve top, pinched waist) — extracted to scratchpad `p0_flame.png`, confirmed by reading it. This is the correct asset to replace the old teardrop-SVG `ICONS.flame` everywhere, per Kell's standing rule NOT to freehand-redraw it. **NOT DONE this session (carry forward):** the actual merge of the three prototypes (P0 onboarding → capture flow) into the single canonical Artifact was only *scripted* (`scratchpad/merge.py` — scopes P0's CSS under `#p0-root`, prefixes the 10 colliding capture classes under `#capture-root`, isolates P0's IIFE JS, bridges s20's completion into `startCaptureFlow()`, swaps in the real flame). The script was **never run, the merged file was never built, verified in-browser, or published.** The flame swap and the merge are both still pending — do not assume either is live in the artifact.

---

**August 23, 2026 session — Combined all 3 prototypes into one continuous click-through, and locked Slide Up:** Kell chose Slide Up as final (over Container Transform, kept selectable for reference) and asked to combine the Onboarding, Onboarding Capture, and Post-Onboarding Capture prototypes into one experience. Built `docs/Dwellable_Full_Prototype.html` by programmatically merging `P0_NavChrome_Prototype.html` + `P1_OnboardingCapture_Prototype.html` (built fresh this session — real content sourced from Figma's `Full Flow (Polished v1)` and `Error States & Edge Cases` pages, replacing a prototype that was described in tickets from an earlier session but never actually existed as a file) + `P1_PostOnboarding_Prototype.html`. Reconciled CSS variable name differences between files, prefixed all screen IDs (`oc-`/`po-`) to avoid collisions, merged both nav engines into P0's single click handler (added `data-overlay`/`data-morph`/transition-mode support P0 didn't have). Caught and fixed two real bugs during assembly: both `oc-entry` and `po-tab-today` carried a hardcoded `active` class from being their own source file's default screen (would have rendered simultaneously); the "Continue to Dwellable" journal-reveal CTA needed rewiring from its original self-loop to actually bridge into `po-tab-today`. Click-tested the full path end to end: welcome → 20-screen onboarding → auto-advances into first Capture → error-state scenario → "Continue to Dwellable" → lands in the real 3-tab app → "+" → Capture (both transition modes verified) → exit. `docs/PROTOTYPES.md` updated with the merge details.

**August 20, 2026 session (later same-day, continued #11) — Transition mechanism re-decided: LOCKED = Slide Up, not Container Transform.** After extended real-world testing (double-take stutter, then a genuine overlap bug from `.screen` having no background, then the dim-behind "appearing" issue), Kell chose **Slide Up** over the earlier Container Transform lock from earlier this same session — the iOS-standard `.fullScreenCover` pattern, cheaper to implement correctly (`matchedGeometryEffect` not needed), and now that the underlying rendering bugs are fixed, feels right. Set as the default `transitionMode` in `docs/P1_PostOnboarding_Prototype.html` (was 'morph'), toggle button markup updated to show Slide Up pre-selected and labeled "✓ Locked". Container Transform stays selectable in the prototype for reference/comparison, not deleted. **Supersedes** the "Transition mechanism — LOCKED: Container Transform" entry earlier in this same session's log — that entry is now historical, not current.

**August 20, 2026 session (later same-day, continued #10) — Removed the dim-behind blackout; the tab underneath should never appear to "load in":** Kell reframed the mental model with a bed/comforter metaphor: the tab (Today/Entries/Growth) is the bed sheet — always there, fully made — and Capture is the comforter laid over it. Pulling the comforter off should reveal the sheet already there, not have it "appear." The previous fix (giving `.screen` its own opaque background) was correct and is what actually prevents any overlap — but the `dim-behind` mechanic (blacking out the tab underneath via `filter: brightness(0)` for the duration of the slide) was left in from before that fix, and was actively causing exactly the "appearing" symptom: the revealed area showed solid black the whole time the sheet moved, then the real tab content snapped in only at the very end when the class was removed. Deleted `dim-behind` entirely (CSS rule and all 4 JS add/remove call sites in `slideToScreen`/`slideOutScreen`) — the tab underneath is now never touched at all, stays fully rendered and visible the entire time, and the sliding sheet's own opaque background is solely what does the covering/uncovering. Verified by pinning the animation at 10-30% through on both directions: the tab's real content (heading, "+" button) is visible above/around the sheet's edge from the first frame, never blacked out.

**August 20, 2026 session (later same-day, continued #9) — Fixed the *real* Slide Up overlap: `.screen` was transparent:** Kell showed screenshots proving Today's text ("9:41", "Today", "+") was still visible above the sliding sheet's top edge mid-transition, even with `dim-behind` applied. Diagnosed by inspecting computed styles: the dim-behind filter WAS applying to `#tab-today` (`filter: brightness(0)`, confirmed via getComputedStyle), but the text was still visible in the screenshot. Root cause: `.screen` had `position:absolute; inset:0` but **no background color** — screens were transparent. Meant that even where the sliding sheet was rendered on top of Today, both screens' content was drawing to the same pixel (both transparent over the phone-screen's #0a0a0a background). The dim-behind filter turned Today's text black, but on a near-black background pure black is *still slightly distinguishable* — the "Today" heading with its ~245-value white color, once brightness(0) applied, became pure black on near-black bg, which is why some text was still faintly visible. Fixed by adding `background: var(--bg-screen)` to `.screen`, so each screen paints its own opaque backdrop — sheet fully covers Today wherever the sheet is, and dim-behind on Today handles the area above the sheet's top edge. Verified by pinning the animation at 30% and screenshotting: entire top area above the sheet is now solid black with zero content visible; sheet's own status bar / back arrow / content renders cleanly below. Applies to both slide directions.

**August 20, 2026 session (later same-day, continued #8) — Polish pass on Slide Up: quicker, symmetric exit, header cleanup:** Kell sent another screen recording (Journey app again, showing its compose screen header for reference) plus 4 concrete asks: (1) speed up the slide (was 0.42s, now 0.3s both directions); (2) exiting should slide DOWN at the same speed it entered, not just instantly swap — added `slideOutScreen()` + a `slideDown` keyframe, wired into the general `data-goto` click handler but scoped specifically to "leaving the capture flow back to a tab" (checked via `dataset.goto.startsWith('tab-')`) so forward progress within the flow (Recording → Transcription, etc.) still stays a plain swap, not an unwanted slide-down; (3) replaced the oversized dismiss "×" with a smaller (12px) left-chevron arrow across all 5 screens that had one; (4) removed the decorative "•••" status-bar dots entirely (`display:none`). Verified the exit slide with the same real-click + frame-sampling method: smooth continuous motion from top:52 (resting) to top:840 (fully off-screen) over ~300ms, matching the entrance speed and direction reversed.

**August 20, 2026 session (later same-day, continued #7) — Fixed the real visual problem Slide Up had: overlapping text with nothing underneath:** Kell sent 4 screenshots showing the actual issue — mid-slide, two status bars ("9:41" doubled), the incoming screen's "×" and "DWELLY" overlapping the origin's "Today" heading, all unreadable. Root cause: both screens share the same near-black background with no visual separation, so while both are partially visible during the slide (correct/expected for a sliding sheet), their text overlaps into a jumbled mess with no way to tell which layer is which. Fixed by (1) dimming whatever screen is underneath to 35% brightness for the duration of the slide (`.dim-behind`, captured via `Array.from(screens).find(...)` on the actual currently-active screen right before the slide starts, cleared in the same cleanup step as the rest), and (2) adding a drop-shadow to the incoming sheet's top edge so there's a visible boundary even where backgrounds are similar. Verified via the same rAF frame-sampling method: the dim class is present for the entire visible duration of the slide (t=3ms through t=426ms) and clears exactly as the sheet reaches rest.

**August 20, 2026 session (later same-day, continued #6) — Fixed Slide Up actually snapping instead of sliding:** Kell reported (after several rounds of "is it rendering at all" troubleshooting — traced to normal browser caching/confusion, not a real bug) that Slide Up "overlays the next screen over top of it, and then the previous screen slowly dissipates" instead of gracefully sliding up — i.e. the new screen was popping in near-instantly rather than animating. Root cause: the same class of timing race as the Container Transform "double take" bug — Slide Up used a manually-timed `transition` (set starting inline state, double-`requestAnimationFrame`, then set end state) instead of a declarative animation, and that JS-timed approach can still let the browser coalesce start/end into one frame under real user-click timing even though it measured fine under synthetic test dispatch. Fixed by replacing it with a real CSS `@keyframes slideUp` animation — declarative keyframes reliably play their `from` state the instant the class is applied, with no JS-timing race possible. Also fixed a second latent bug this surfaced: the `sliding` class was never removed after use, so a *second* visit to the same screen wouldn't animate at all (re-adding a class an element already has is a no-op) — fixed with the standard remove/reflow/re-add restart pattern. Verified via real click + frame sampling: smooth continuous motion from top:840 (fully off-screen) to rest on both the first AND second entry.

**August 20, 2026 session (later same-day, continued #5) — Removed the border entirely + added Slide Up as a live, directly-comparable toggle:** Kell: (1) didn't want the gold border at all, not just faded out after settling — removed `border` from `.screen.morphing` entirely rather than fading it via the `morph-done` class from the previous fix (that class/mechanism is gone now, no longer needed since there's nothing to fade). (2) Found Container Transform "abrupt" at the bottom of the screen, conflicting visually with the tab bar/text-field area, and asked to actually see Slide Up again — not as a separate demo artifact, but live in this same real prototype so the two are directly comparable with real content. Added a `"+"` **transition toggle** in the hub panel (Container Transform / Slide Up); the 3 plus-buttons now dispatch to whichever `transitionMode` is selected. Slide Up implemented as a plain `transform: translateY(100%→0)` — structurally can't have the earlier shrink bug at all, since no width/height/layout properties are touched, only a GPU-composited transform. Verified via the same frame-sampling method: zero width/height anomalies across the full transition.

**August 20, 2026 session (later same-day, continued #4) — Fixed a real "double take" stutter in the Container Transform, root-caused via measurement rather than guessing:** Kell reported the "+" animation stuttered / looked like it opened twice, and shared a screen recording — which turned out to be in a macOS-protected `NSIRD_screencaptureui` temp path this session couldn't read (no permission; needs saving to Desktop/Downloads to be shareable). Rather than wait, reproduced and root-caused it directly by sampling `getBoundingClientRect()` every animation frame during the transition. Found the box grew smoothly and correctly to its full 351×788 size, then at the 520ms cleanup step **shrank to 344×772 and regrew** over ~120ms — a real second micro-animation, matching "double take" exactly. Cause: the cleanup step removed the `morphing`/`morph-open` classes (which strip the border and `overflow:hidden`) and cleared inline styles in the same tick as the transition's own class/property changes — even though the *computed* CSS width stayed 351px throughout, the actual rendered box measurably dipped. Fixed by never removing those classes at cleanup (the next morph always resets them fresh anyway, so leaving them is inert) and fading the leftover border out via a separate `morph-done` class that only touches `border-color`, isolated from the properties that caused the glitch. Reverified via the same frame-by-frame measurement: zero shrink events across a full 900ms run, border correctly fades to transparent.

**August 20, 2026 session (later same-day, continued #3) — Fixed the "+" transition to actually be Container Transform:** Kell caught that the HTML prototype's "+" button just used the same generic fade/scale-in as every other screen change — not the Container Transform that was explicitly locked earlier this session. Fixed in `docs/P1_PostOnboarding_Prototype.html`: the 3 plus-buttons now use `data-morph` (not `data-goto`), handled by a new `morphToScreen()` function that reads the button's actual on-screen rect via `getBoundingClientRect()`, pins the Capture Entry screen exactly over it (39×39, button's border-radius), forces a reflow, then transitions top/left/width/height/border-radius to full-screen over ~500ms — a real CSS size/position interpolation, not a instant class swap. Verified via `getBoundingClientRect()` sampled mid-animation (39×39 → 303×674 → 349×784), confirming genuine interpolation rather than a disguised cut. Every other screen-to-screen navigation keeps the plain fade — only the "+" → Capture Entry moment uses the morph, matching what was actually locked.

**August 20, 2026 session (later same-day, continued #2) — Reconciled two different "save" screens in the HTML prototype into one:** Kell caught a real inconsistency: finishing a conversation naturally and leaving early via the "Start over?" modal's "Save Moment" button led to two completely different confirmation experiences — the invented 3-screen "Entry Saved → Creating Your Journal → Journal Reveal" sequence for early exit, versus nothing consistent for natural completion. Root cause: the real, already-designed onboarding screens `Header Done — Natural Completion` and `Header Done — Early Exit` (found earlier this session, both showing Dwelly delivering an inline message + embedded journal-preview card, no separate interstitial screens at all) were never actually used — the 3-screen sequence was invented from scratch instead. Fixed by replacing both `saved`/`creating` screens in `docs/P1_PostOnboarding_Prototype.html` with two inline "Done" screens (`done-natural`, `done-early`) matching the real Header Done pattern exactly, differing only in Dwelly's copy; both link out to the same full `journal-reveal` screen via an "Open" tap for anyone who wants the complete view. Click-tested live in-browser, both paths confirmed working correctly.

**August 20, 2026 session (later same-day, continued) — T-076 (Menu/Navigation) designed in Figma, Pillar 1 Figma pages reorganized into Onboarding vs. Post-Onboarding:** Resolved T-174 ("Pillar 6" naming collision) per Kell's explicit call — Pillar 6 = Menu/Navigation, overriding the July 11 reassignment to Formation Intelligence (flagged unresolved follow-up: T-160–T-164 and `PILLAR_6_SEARCH_STRATEGY.md`/T-HYP-P6 still also claim "Pillar 6" and need renumbering). Built a new Figma page `P6 — Menu / Navigation`: a `TabBar` component with 4 tabs (Today | Entries | Capture | Growth — 3rd tab renamed from "Create" to "Capture" per Kell's reference), redesigned mid-session from an initial full-width bar to a floating rounded-pill bar with custom icons (ring+dot, open-book, outlined circle+plus, ascending bars) after Kell supplied a reference screenshot to match exactly. Composed 4 example screens (Today/Entries/Capture/Growth placeholders) to prove the shell. **Then reorganized all 9 existing Pillar 1 Figma pages** — renamed each to make clear they're onboarding-context Capture designs (e.g. `P1 — Save Confirmation (Polished)` → `P1 — Capture Onboarding — Save Confirmation (Polished)`) — and **created a new page `P1 — Capture — Post-Onboarding`** for the return-visit Capture flow (reached via the new Capture tab), building 4 screens there: Capture Home (rotating-prompt entry, Rich Context caption), Voice Recording Active, Dwelly Conversational Review (chat-bubble exchange), and Saved/Journal Transition — every screen carries the Capture-active TabBar per Kell's explicit instruction that all post-onboarding Capture screens include the menu shell. Design only — no SwiftUI code touched this session (T-076 still 🔲 Not Started, design pass complete).

**August 20, 2026 session — Pillar 1 (Capture) error states & edge cases fully integrated into the working HTML prototype + 1 new ticket raised:** Extended `capture_flow_prototype.html` (same published artifact used for the happy-path build) with all 8 error/edge-case scenarios from the Figma error-states file, each wired directly into the real state machine rather than built as isolated demos: mic permission denied (system alert → in-app fallback modal → text), transcription silence (1st attempt "try again" → 2nd attempt escalates to "try typing instead"), technical transcription failure (retries same retained audio → same 2nd-attempt escalation as silence), max recording duration (silent auto-stop at 10 min → full-screen long-transcript review with expand/collapse and a mic-as-resume affordance that appends rather than restarts), Dwelly going offline mid-reply (two distinct paths — reconnects before the capture finishes, vs. stays offline with completion only possible via the "Done" early-exit button — persistent red/green status banner either way), and Cancel-vs-Stop while recording (real bug fix: Cancel had no handler of its own and silently behaved like Stop; now instant-discard, no confirmation, per spec). Every scenario demo-toggleable and re-verified end to end against the Figma reference screens, including several rounds of pixel-level correction (icon direction/geometry pulled from the source file's actual vector rects, not approximated; scroll-fade + structural fix so mic/send controls can never overlay scrolled text; full-bleed vs. partial expand). Also did a first pass on Pillar 6 (Menu Bar / Navigation) sequencing — see rationale in T-172 and the Next Session Objective below. **New ticket raised:** T-172 (port the now-prototype-validated Capture error-state behaviors into the actual Swift `CaptureView.swift` — today's work is HTML/JS prototype only, not yet reflected in the native app).

**August 4, 2026 session (continued) — Reader-paced onboarding animation built end-to-end + 2 new tickets raised:** Built and confirmed screen-by-screen bespoke timed-reveal pacing across `docs/P0_NavChrome_Prototype.html` (s05-s20): narrated text reveals for the demo/story screens (s05-s15), an animated recording card with real waveform + independent 15s loop (s11), Dwelly and letter "thinking" typing indicators (s12, s14), interactive gating fixes (s16 no pre-checked cards + tap-continue CTA gated on selection; s19 real form inputs + CTA gated on fields+terms), s16-s19 changed to show fully populated immediately (no stagger) while s05-s15 keep narration pacing, an auto-advancing 4-step loading sequence (s20), and live client-side validation on s19 matching the archived prototype's exact error-state designs (email format, password length, confirm mismatch). All merged into one file end-to-end verified via full click-through with zero console errors. **New tickets raised:** T-170 (review onboarding pacing against real Pillar 1 capture flow) and T-171 (review s20 loading-screen copy — currently implies SDK downloads that don't actually happen at runtime; see rationale in ticket).

**August 4, 2026 session — Nav-chrome HTML prototype built and iteratively corrected against live Figma; no engineering ticket status changes (pure design/prototype):** Built `docs/P0_NavChrome_Prototype.html`, a standalone 20-screen click-through HTML port of the nav-chrome copy row (back button + login link work from the prior session) in `t5MUGEtpeFcUixobvHiYMc`, page `P0 - New Onboarding`. Added the real `welcome-screen` cover (logo, wordmark, Get Started) that had been missing from the row entirely. **Extensive fresh-pull correction pass across most screens**, catching and fixing real drift between my build and the live Figma file: wrong/missing fonts and weights (Instrument Serif vs Sans, several missing Medium/SemiBold weights) verified via `getStyledTextSegments`; wrong colors (nav-login chrome using the wrong CSS variable); emoji icons swapped for the actual SVG vectors on `payoff-h-scroll` (leaf/box/compass/heart/star), `10-intent-selection` (message-circle-check), `12-privacy` (shield-check/lock-keyhole/circle-x/message-circle), `13-account-creation` (real Apple/Google logos, eye-off, checkmark), and `14-preparing-space` (checkmark + loading ring, plus a missing home-indicator bar); wrong/placeholder images replaced with real Figma-exported assets on `07f-journal-entry` (photo) and `11-gentle-reminders` (notification icon — this one had been edited in Figma same-day mid-session, causing a real stale-pull miss, not a caching issue as first assumed). **Structural rebuilds, not just polish:** `11-gentle-reminders` and `12-privacy` and `13-account-creation` had all drifted from "big CTA button + login link" to a plain "tap to continue →" pattern in the live file since these were last built — rebuilt all three from a full fresh pull rather than patching. Fixed real layout bugs: back-button-hidden-behind-headline on `10-intent-selection` (root cause: both shared the same y-coordinate — content needed a top offset, not a z-index fix), `tap-to-continue` clipping past the rounded corner on 3 content-heavy screens (root cause: flex-flow positioning instead of pinned-absolute — converted all 3 to the pinned pattern used everywhere else), and vertical centering corrected on `07b-the-old-way` and `14-preparing-space` after confirming via exact Figma coordinates that centering was the real design intent, not just a preference. Made `10-intent-selection`'s and `11-gentle-reminders`' option cards genuinely interactive (checkbox multi-select / radio single-select), verified via simulated real clicks. **Key process lesson, logged for next session:** several of these screens were built once early in the session and never re-verified against a fresh Figma pull as the file kept changing — leading to multiple rounds of "this doesn't match" from Kell. Going forward, pull each screen's full spec fresh rather than assuming it still matches an earlier pull or a similar sibling screen.

---

**August 3, 2026 session — Pillar 3 (Prayer) moved to Post-MVP; MVP loop redefined as Capture → Process → Journal; no engineering ticket status changes (pure scope decision):** Reviewing the merged Problem-First onboarding flow (`t5MUGEtpeFcUixobvHiYMc`, y=1265 row), Kell flagged that the demo-loop screens (07c-voice-the-moment → 07d-dwelly-processes → 07f-journal-entry) skip the prayer step (`07e-dwelly-prays`) entirely. Confirmed this is intentional, not an oversight: **Kell is deliberately narrowing MVP scope to three things — Capture, Process (the Dwelly conversation), and Journal** — judging that shipping even those three well is already significant scope, and that Prayer is a nice-to-have layered on top of an already-complete loop (you can capture and journal without ever praying over a moment), not essential to validate the core hypothesis. Prayer (Pillar 3) reclassified from "Design Complete, Implementation Not Started" to **Post-MVP/Deferred**. All existing Pillar 3 design work (2-option skeleton, Rich Context integration, Gallery/Soak Mode/Reflection Prompts architecture) remains valid and unchanged for whenever it's picked back up — only MVP sequencing changed, not the design itself. Updated `docs/PRD.md` (Pillar 3 status line + new MVP-core-loop callout in Build Phases). **Downstream note for next session:** Pillar 8's Stage C–G notifications explicitly depend on "Pillar 3 shipping first" per the July 20 reclassification note (line ~496) — that dependency framing may need a second look now that Prayer isn't in MVP at all, not just sequenced after it. Not resolved this session; flagging only.

---

**August 1, 2026 session — P0 prototype rebuilt pixel-exact + made real, competitor teardown (Prayer Lock), new "Problem-First" onboarding exploration built in Figma; no engineering ticket status changes (pure design):** Three threads this session, all in `docs/P0_prototype.html` and Figma file `t5MUGEtpeFcUixobvHiYMc`. **(1) HTML prototype corrected from approximated to exact:** Kell flagged the prototype didn't actually match Figma — audit confirmed the CSS had been hand-estimated, not pulled from `get_design_context`. Rebuilt all 20 screens against exact Figma values (padding, gaps, font sizes, the real three-zone `justify-between` layout Figma actually uses) and fixed real bugs along the way: welcome screen's flame logo overlapping the headline (twice-adjusted after Kell caught the first fix undershooting), wrong icons on `what-dwellable-does` + `privacy` (emoji swapped for correct line icons matching the design), wrong accent color on the intent/rhythm-selection acknowledgment icons, wrong app icon in the `gentle-reminders` notification preview, and — the most consequential bug — the name-entry field was static decorative text, not a real input; replaced with an actual `<input>` wired so the typed name flows into the next screen's headline. Also discovered intent-selection/rhythm-selection's option rows had zero click handlers at all (pure visual mockup) — added real checkbox/radio toggle logic with auto-gating on the Continue button (disabled until ≥1 selection), including on the two "none selected" edge-case screens that were supposed to demonstrate exactly this state. **(2) Flow reorder — done in the wrong file, not yet applied to the real page:** Per Kell's UX critique (personalize before pitching the value prop; show privacy before asking for notification permission), rewired the screen order and discovered these Figma frames have real click-through prototype reactions (not just visual placement) — fixed progress-dot indicators and reaction destinations to match the new order. **This was built entirely in a duplicate "Untitled" file Kell had shared for a separate review, not the real `P0 - New Onboarding` page** — caught mid-session, and the reorder has not yet been reapplied to the actual production page. **Open decision for Kell: confirm whether to apply it there.** **(3) Competitor research + new exploratory page:** Read the HubSpot "Mobile App Onboarding Deep Dive" case study (Prayer Lock, Mau Baron — 5→29 screens took conversion from 3% to 12-15%) and cross-referenced it against Kell's own screen recording of the live current app, which turned out meaningfully richer than the article describes (illustrated shepherd/lamb art, a 90-day journey grid, two-beat positive reframes, social proof, a review-ask timed at the emotional peak rather than the end). Built a **`Prayer Lock — Coverage Map`** page (14 themes, each tagged COVERED/PARTIAL/GAP/SKIPPED/ELSEWHERE against Dwellable's design) and a new **`Onboarding — Problem-First Test (Prayer Lock)`** page — an experimental problem-first sequence in Dwellable's own voice, not Prayer Lock's: a memory-decay aha stat instead of screen-time math, no age/usage-collection screens since our stat doesn't need them. Iterated through several rounds of feedback in-session: added and then cut a "52 reminders" quantified reframe screen, added a "wall of encounters" vision screen (Kell rejected an initial "365 days from now" framing — rewritten as a reflective question instead, kept the visual grid since that part worked), and added a mechanism-explainer screen in **two variants for comparison** — `07a-mechanism-text` (prose) and `07b-mechanism-visual` (the same moment-card shown twice, muted-then-gold, demonstrating capture→reflect instead of describing it), both left in the click-through chain back to back so Kell can compare live in Presentation mode. Current test-page sequence (10 screens, fully wired): cold-open → problem → solution → name-entry → personalized-interstitial → aha-moment → 07a-mechanism-text → 07b-mechanism-visual → wall-question → bridge. **Open decisions for Kell:** pick 07a vs. 07b (or keep both longer), and decide whether this test page's direction should replace the current locked P0 flow or stay a reference exploration.

---

**July 31, 2026 session (extended a seventh time, same day) — Standalone HTML prototype built as a faithful port of the Figma prototype; no engineering ticket status changes:** Built `docs/P0_prototype.html`, a single self-contained file (fonts embedded as base64 data URIs — real Instrument Sans variable font + Instrument Serif fetched from Google Fonts, not a CDN link that would break under strict CSP or risk a silent fallback). Same Onboarding/Gold tokens, same 40-connection wiring map from the earlier Figma audit, same hub-plus-happy-path-plus-9-edge-cases structure. Caught and fixed one real bug before shipping: the hub's click handler was resetting navigation history on every jump, silently breaking every edge-case screen's Back link — fixed by deleting the redundant handler (hub links already shared the same `data-goto` mechanism as everything else). Verified via direct JS injection since the Browser pane's simulated clicks don't register on this file (renders as a static snapshot for `file://` URLs outside the project folder) — all navigation, the Back-link fix, and the modal overlay confirmed working against live DOM state. Delivered both as a committed repo file and published via Artifact for mobile/shareable viewing.

---

**July 31, 2026 session (extended a sixth time, same day) — Full prototype wiring audit; no engineering ticket status changes:** Kell reported apparent checkbox toggling in Play mode. Directly inspected the intent-selection/rhythm-selection checkbox/radio elements and confirmed zero reactions exist on any of them — likely explanation is navigating between the happy-path screen (pre-checked) and its "none selected" edge case via the hub reads as toggling since they're separate fixed-state frames, not one live screen; Kell confirmed this is the intended behavior when asked to choose between that and building genuine live toggle interactivity (a bigger, riskier build using less-documented Figma variable/conditional-logic prototyping features). Ran a full audit of all 40 wired reactions across all 21 screens — zero broken destinations. Found and fixed one real imprecision: `gentle-reminders`' "Not now" link had attached to an oversized parent container instead of itself (same destination, but too-large a clickable area) — tightened to the precise text node.

---

**July 31, 2026 session (extended a fifth time, same day) — Full interactive Figma prototype built covering happy path + all 9 edge cases; no engineering ticket status changes:** Kell ran the QA checklist and confirmed it clean, then requested a real click-through prototype. Discovered a hard Plugin API constraint mid-build: `NAVIGATE` actions only accept same-page destinations, so the 9 edge-case screens (previously on a separate `Onboarding — Edge Cases` page) had to be moved onto the same page as the happy path (`P0 — New Onboarding`) before cross-linking would work at all — first placement attempt also collided with the happy-path row's x-range and needed repositioning to its own row. **Wired:** full linear happy-path chain (Welcome through the loading screen, with the moment-example modal opening as a proper OVERLAY and closing via CLOSE); a new `Prototype Start` hub frame linking into the happy path plus all 9 edge cases directly (since most error states aren't reachable by tapping something on the happy path); a `← Back` link on every edge-case screen using the BACK action; and 3 bonus hotspots on account-creation's actual Email/Password/Confirm fields jumping to their respective format-error screens. Two more bugs hit and fixed: setting `.reactions` before `appendChild()` throws (fixed by reordering); back-links initially landed off-screen due to the same auto-layout-override problem hit earlier this session, this time properly fixed with `layoutPositioning = 'ABSOLUTE'` rather than fighting the flow. Usage: open the file, select `Prototype Start`, enter Presentation mode.

---

**July 31, 2026 session (extended a fourth time, same day) — All 4 open edge-case questions resolved, Terms-error state removed, checklists updated; no engineering ticket status changes:** Kell answered all 4 open questions from the QA checklist. (1) **Duplicate email → deep-links to login** — "log in instead?" now underlined as a link affordance. (2) **Terms/Privacy → no separate error state exists** — Kell clarified Create Account simply never enables until all 4 required items are complete, so there's no user action that could trigger a distinct "submitted incomplete" error; **deleted** the standalone red "Terms not accepted (error)" screen built earlier this session as inconsistent with that model, added a "Complete all fields to continue." hint to the plain default screen instead (matching intent/rhythm's existing hint pattern). Edge-case count back to 9 (was 10). (3) **Network error → recommended silent auto-retry before showing, manual-only after, and applied the button-re-enable fix** — the Create Account button now stays gold/enabled on this screen instead of disabled, since the failure is server-side, not a field problem. (4) **moment-types-loading retry → recommended resume-from-failure over full restart**, flagged as contingent on backend feasibility (not purely a design call). Both `docs/P0_EDGE_CASE_QA_CHECKLIST.md` and the in-Figma checklist frame updated to match. **Caught the same default-white-fill bug a fourth time** (this time on the checklist's own section-frame wrappers) — fixed immediately, logged as a pattern needing more deliberate handling going forward, not just a known rule.

---

**July 31, 2026 session (extended a third time, same day) — QA checklist added to Figma, new Terms/Privacy error state, loading-screen layout fix; no engineering ticket status changes:** Kell reviewed the work directly in Figma and requested 3 more fixes. (1) Built a `QA Checklist` frame directly on the `Onboarding — Edge Cases` page (not just the repo doc) covering all 10 edge cases with checkbox-style rows. (2) Added a distinct **Terms/Privacy not accepted (error)** state (node `276:3`) — checkbox + label turn red, explicit error message shown — separate from the existing plain "unchecked" default state; flagged an open question on what interaction actually triggers it given the button is already disabled until checked. (3) **Fixed a real layout bug** on `moment-types-loading — failed`: the retry button was flush-left with zero clearance, crammed inside what was meant to be reserved bottom safe-area padding. Root cause: the screen uses nested auto-layout, so directly setting the button's x/y silently did nothing (auto-layout overrides manual coordinates every layout pass) — fixed by setting parent `itemSpacing`/`paddingBottom` and wrapping the button in a padded container matching the screen's own existing inset pattern, working with the auto-layout system instead of against it. Edge-case screen count now 10 (up from 9). Both `docs/P0_EDGE_CASE_QA_CHECKLIST.md` and the in-Figma copy updated to match.

---

**July 31, 2026 session (extended again, same day) — 12 happy-path screens rebound to design tokens + manual QA checklist delivered; no engineering ticket status changes (pure design):** Closed both remaining Pillar 0 follow-ups. **Rebinding surfaced 3 real bugs, not just mechanical rewiring:** (1) a variable-collection name collision — the stale cream-theme `Onboarding/Colors` collection and the new `Onboarding/Gold Colors` collection both have variables named `text/heading` etc., and a naive lookup grabbed the wrong (near-black) one, briefly making some `what-dwellable-does` text unreadable until caught and fixed by scoping lookups to the correct collection; (2) applying the badge text style to `intent-selection`'s tiny checkmark glyphs distorted them — fixed by excluding short glyph-text from full-style application; (3) `account-creation`'s field labels lost their `UPPER` textCase transform when styled — fixed on both the style definition and the affected nodes. All 12 screens re-verified via full visual sweep after fixes — pixel-matching original appearance except the intended corrections (icon-tint standardization, DM Sans→Instrument Sans, radius 26→24, all per earlier-locked decisions). **QA checklist delivered:** `docs/P0_EDGE_CASE_QA_CHECKLIST.md` — per-screen checkboxes + direct Figma links for all 9 edge-case screens, plus 3 flagged open product questions (duplicate-email deep-link behavior, network-error retry UX, loading-failure retry scope) for Kell to resolve before engineering handoff. **Pillar 0 is now fully complete with nothing outstanding.**

---

**July 31, 2026 session (extended, same day) — Design system + all edge/error states built for Pillar 0; no engineering ticket status changes (pure design):** After locking the 12-screen happy path (see entry below), built out two more deliverables Kell requested before moving to Pillar 1. **Design system audit found real drift:** an existing-but-unused `Onboarding/Colors` variable collection (old cream theme) had never been bound to the current `-gold` screens — every color was raw/hardcoded. Full inventory across all 12 screens surfaced a stray third font (DM Sans, ~6 instances, everything else uses Instrument Sans/Serif), 8 different uncontrolled headline sizes, a 24-vs-26 corner-radius split, and inconsistent icon-circle tinting on `what-dwellable-does`. Kell resolved 4 real decision forks (icon-circle standardized to neutral; password = 8+ chars no complexity; error copy = plain system text, not Dwelly-voiced; required-selection screens block Continue until ≥1 chosen). **Built:** `Onboarding/Gold Colors` (19 tokens) + `Onboarding/Gold Metrics` (20 tokens) variable collections, 11 text styles, and 10 components (`DwellyBadge`, `ProgressDots`, `PrimaryButton`, `IconCircle`, `StatementCard`, `SelectableCard`, `TextInput`, `CheckboxTerms`, `StatusBar` — `ToggleRow` turned out unnecessary, `gentle-reminders` reuses the same radio pattern as `rhythm-selection`) on a new `Onboarding — Components` page. Two bugs caught and fixed mid-build (both `createAutoLayout`/`createFrame` defaulting to an opaque white fill on wrapper frames that were never explicitly cleared — `StatementCard` and `ProgressDots` both hit this). **Then built 9 edge-case screens** on a new `Onboarding — Edge Cases` page using real component instances: `account-creation` × 6 (email-format error, password-too-short, confirm-mismatch, duplicate-email, terms-unchecked, network/server-error banner), `intent-selection` + `rhythm-selection` × 1 each (zero-selected/Continue-disabled), `moment-types-loading` × 1 (load-failure + retry). One more bug caught and fixed (network-error banner text overflowed the screen edge — unconstrained width). **Known minor nit, not fixed:** intent/rhythm's acknowledgment copy lines read oddly in the zero-selected reference state — cosmetic, flagged for a future product call, not blocking. **Pillar 0 is now fully complete: happy path + design system + edge cases.** Two small non-blocking follow-ups noted for whenever convenient: the copy nit above, and the 12 happy-path screens still use their original raw values rather than being rebound to the new tokens (visually identical today, just not wired for propagation).

---

**July 31, 2026 session (close) — Pillar 0 (Onboarding) design LOCKED COMPLETE at 12/12 screens; no engineering ticket status changes (pure design), doc corrections made:** Verified the live Figma file directly rather than trusting July 30's memory, and found two errors: the canonical row is actually the **`-gold` suffix row at y=1388** (ids `248:xx`), not the unsuffixed row at y=2498 recorded previously — that row plus a `-dark` suffix row at y=8730 are confirmed **archived** by Kell. Also corrected the font system on record: the file actually uses **Instrument Serif** (headlines) + **Instrument Sans** (body/labels), not Cormorant Garamond + SF Pro as previously documented. Audited all 13 named screens in the canonical row against live screenshots: 10 were already fully built (welcome, what-dwellable-does, name-entry, intent-selection, rhythm-selection, gentle-reminders, privacy, account-creation, moment-types-loading, moment-example-modal); `dwelly-intro-gold` and `personalized-transition-gold` have empty bodies below their headline/CTA but Kell confirmed these are **intentionally minimal transition beats**, not gaps; `capture-method-gold` did not exist at all. Built `capture-method-gold` by cloning the archived cream `capture-method` frame (preserving its mic/keyboard icon vectors) and re-theming it to match `privacy-gold`'s exact style tokens (near-black `rgb(0.04,0.04,0.04)` container, gold accent `rgb(0.788,0.698,0.478)`, Instrument Sans/Serif type). Progress dots confirmed Pillar 0 tracks **12 steps** (moment-example-modal is a modal on moment-types-loading, not its own step) — capture-method was correctly the final/12th dot. **Kell then removed the screen from Pillar 0** — capture method selection (voice vs. type) belongs to **Pillar 1 (Capture)**, not Onboarding. The design work isn't lost: same content and dark/gold styling just needs a new home in Pillar 1's Figma section whenever that pillar's design work is picked up (archived cream reference still at node `220:538` in file `t5MUGEtpeFcUixobvHiYMc`). **Pillar 0 now stands at 12/12 screens complete, no open design items.** Root-cause note for future sessions: this Figma MCP is a desktop-app bridge (reads whatever file is open in Figma desktop locally), not a cloud API by file key — an empty/blank `get_metadata` result on a documented file key means the file isn't open locally, not that it's actually empty; ask for a live node-id URL from the open file.

---

**July 30, 2026 session (close) — Full Pillar 0 Onboarding row designed end-to-end in Figma; no engineering ticket status changes (pure design), two backlog items added:** Built and iterated a complete 9-screen onboarding flow in the `t5MUGEtpeFcUixobvHiYMc` Figma file (`P0 — Onboarding` page): Welcome, Education, Privacy, Intent, Rhythm, Notifications, Account, Name, Building-your-account. Established a proper design system foundation partway through — created a new `Onboarding/Colors` Figma variable collection (13 tokens: bg/cream, surface/card, surface/badge-tan, surface/button-dark, text/heading, text/body, text/muted, text/on-dark, accent/gold, accent/brown, border/subtle, border/radio-outline, track/unfilled) after discovering my earlier work was drifting between hand-typed color constants; rebound all screens' fills/strokes to these shared tokens so future edits propagate consistently. **Font system unified:** locked Cormorant Garamond for headings/wordmarks (previously drifting between Cormorant, Noto Serif JP substitute, and inline sizes) + SF Pro for all body/subhead/button text; ran a 4-variation Fraunces/Lora/EB Garamond/Cormorant comparison before Kell locked Cormorant + SF Pro. **Icons:** used real Phosphor Icons library assets (Kell added the library to the file mid-session) for leaf/praying-hands/book on the Education screen; remaining icons across other screens (envelope, lock, eye, shield, key, sparkle, bell, moon, thought-bubble, dove, heart, sparkle-burst, sprout, car) are hand-drawn vector approximations pending swap when time allows. **Screen order restructured:** grouped FYI-first (Welcome → Education → Privacy) then Input-cluster (Intent → Rhythm → Notifications → Account → Name → Building) after Kell called out the sporadic FYI/Input interleaving; progress bars swept from /8 to /9 denominators after Name screen added to unblock T-136's Today-tab greeting dependency. **Building-your-account screen** (post-Account, pre-Capture): went through multiple concept iterations — spinner + horizontal loading bar, then pill-scatter of God-moment categories, then a list-card version with valence-paired examples ("peace in hardship", "comfort in a tense season", "joy in a new beginning"), then Kell added a decorative textured background asset and refined copy to "When the Lord breaks through the fog" / "A God moment is when God meets you in ordinary life and something about His presence, care, or invitation becomes clear." A modal example was designed for the tap-to-explain interaction on each pill (icon + title + Example section + Reflect section + CTA). **Ontology work:** worked through the definition of "God moment" at the molecular level with Kell — landed on it being a recognition-based act (person notices God in the ordinary) rather than a category-based event, mapped to three axes: domain (unbounded), valence (unbounded), channel (5 non-exhaustive: internal, devotional, communal, circumstantial, sudden-revelation) — the 5 pills on the Building screen span all 5 channels, not by coincidence. **Conversational-copy experiment:** built a full-flow comparison row (duplicating all 9 screens into a separate row below the live flow) applying Abbey-app-style acknowledgment beats to each headline ("Nice to meet you.", "Got it. How often...", "One more thing —"); first pass had font-overflow bugs (headlines were fixed-height, longer text overlapped subheads), fixed by switching all headlines to auto-resize HEIGHT + unified 26-28pt sizes + dynamic subhead repositioning; not yet adopted into the live flow — Kell to decide. **Kell then restructured the entire file himself,** consolidating our work into a clean new 13-screen row at y=2498 with proper descriptive names: `welcome, dwelly-intro, what-dwellable-does, name-entry, personalized-transition, intent-selection, rhythm-selection, gentle-reminders, privacy, account-creation, moment-types-loading, moment-example-modal, capture-method` — introduced 3 new concepts not in the earlier row (dwelly-intro, personalized-transition, capture-method) that are canonical going into next session. **Backlog items added:** BL-002 (Moments Animation + Background SDK Init as a candidate replacement/expansion of the Education screen — cannot happen literally during App Store download, has to be at first launch on-device); BL-003 (Voiceover Narration for the Building screen using ElevenLabs — postponed, with a draft script and full technical scope caveats about Figma having no audio playback in prototyping, needing a mute/skip affordance from day one, and localization cost). **Figma file state at close:** 4 pages (Page 1 empty, As-Built (Phase 1), P0 — Onboarding, Archived — Photography Carousel); P0 page now organized around Kell's new 13-screen row at y=2498 (canonical), plus older iterations from earlier sessions at higher y-values. **Session frustrations logged for next agent:** Kell called out multiple times when my batch edits shipped without per-screen verification — most notably a headline-copy sweep that broke overflow on 4+ screens I didn't catch until he flagged it. Rule for next session: verify every screen's visual after any batch text/font/layout change, not just spot-check a couple.

---

**July 28, 2026 session (close) — Pillar 0 Onboarding design work in Figma (no ticket status changes — pure design exploration):** Built an "as-built" reference Figma file (`Dwellable — Existing Experience Baseline`) documenting all 7 live Phase 1 screens plus the voice-capture setup/permission/recording states, framed in real iPhone 17 Pro bezels, using Figma variables bound to `Theme.swift`'s actual color/spacing tokens. Then began new Onboarding designs on a `P0 — Onboarding` page: a screen inventory table cross-referencing the locked 7(+6.5)-screen sequence in `docs/PILLAR_ONBOARDING_STRATEGY.md` against open tickets (flagged T-101/T-103's copy-rewrite notes, T-096's open legal review, and MEMORY.md's unresolved P0 Notion comments #5-7). Built two full 9-screen comparison flows (Option 1: existing dark/gold identity; Option 2: photography-band variant), added a Name screen ("What should I call you?") not in the original spec, and a progress bar across all screens — both directions inspired by Untold (multi-select solid-fill clarity) and a reference app's progress-bar/name-entry pattern, explicitly scoped as tonal inspiration only, not a spec override for the two decisions that stay locked (late account creation, mandatory no-skip First Capture). Produced a Figma-AI comparison prompt (`figma_ai_onboarding_prompt.md`, delivered to Kell, not committed to repo) so Kell can independently compare a Figma-AI-generated take against this session's manual designs. Later pivoted to a real-photography carousel direction per Kell's own reference images (4 screens, replacing Welcome+Education) — archived as "Archived — Photography Carousel (Jul 28, v1)" on its own Figma page (nothing deleted) once Kell decided to restart Screen 1 from a blank background-only state and rebuild it piece-by-piece (logo → headline/subtitle → dots/CTA/login) against a specific new reference image. **Two real logo assets discovered and evaluated:** `Dwellable Logos/Extract Logo from Image.svg` (icon+wordmark combined, renders cleanly) and `Dwellable Logos/Dwellable Logo.svg` (icon only, fragile Canva mask-export structure — the real icon graphic is nested inside a raster-image group with a decoy white backing-plate vector as a sibling; easy to grab the wrong element on the first pass). **Session ended mid-swap with a bug**: replacing the Dwellable Logo.svg icon with the Extract Logo icon on the new Screen 1 left the screen rendering fully blank (white) — root cause not yet diagnosed. **First thing next session: fix that blank Screen 1 before continuing the step-by-step rebuild** (logo already established as working from earlier in the session — likely a reparenting/z-order mistake in the last script, not a font/asset problem). Figma file: `t5MUGEtpeFcUixobvHiYMc` (`Dwellable — Existing Experience Baseline`), pages: `As-Built (Phase 1)`, `P0 — Onboarding`, `Archived — Photography Carousel (Jul 28, v1)`.


---

**📦 Older session-log entries (July 23, 2026 and earlier) archived to [`TICKETS_ARCHIVE.md`](TICKETS_ARCHIVE.md) to keep this file lean for session-start reads.** Read the archive only if you need historical detail on a specific past session.

---

## 📋 BACKLOG — Future Features & Pillars

### BL-003: Voiceover Narration for Building-Your-Account Screen
**Pillar:** 0 (Onboarding)
**Status:** 🔲 BACKLOG (Concept + draft script only — postponed)
**Added:** July 29, 2026

**Feature Description:**
Add a voiceover narration to the "Building Your Account" screen (the pill-scatter screen showing the 5 God-moment categories while account/SDK setup happens in the background). Pills would appear/highlight in sync with narration beats instead of (or in addition to) a fixed stagger animation.

**Explicitly postponed this session** in favor of shipping the pill animation alone first. Kell plans to generate a draft voice track via ElevenLabs separately.

**Draft script (needs tightening for spoken delivery once real audio is produced):**
> "Welcome to Dwellable. Thank you for trusting us with your walk with the Lord.
> While we prepare your space... let's talk about what a God moment can be.
> It could be a dream. A vision. A moment in prayer — when something convicts you, or brings you joy, or even leaves you confused, or angry.
> It could be a realization that hits you out of nowhere.
> God moments happen all the time. Any time God breaks through the fog to meet you — that's your moment with Him."

**Important scope note:** This is an audio-production + engineering task, not a Figma task. Figma has no native audio playback in its prototyping model — a design tool pass can only produce a storyboard/timing spec, not a playable preview. Once a real audio file exists:
- Need the finished audio file, ideally with a timestamp/caption export (SRT/VTT or word-level alignment) so pill-reveal timing can be spec'd precisely against real timestamps rather than estimated.
- Actual implementation is SwiftUI + AVFoundation (`AVAudioPlayer`-driven reveal sequence), part of Pillar 1 build work, not this design file.

**Also flagged, not yet resolved:** needs a visible mute/skip affordance from day one (auto-playing audio conflicts with silenced phones, quiet environments, and VoiceOver/accessibility), and a localization cost consideration (recorded voice needs re-recording per language; text/pills should remain a working fallback on their own).

**Dependency:** Should reuse whatever final 5-category pill list ships in the non-voiceover version of this screen (currently: dream or vision, moment in prayer, something you read or heard, quiet thought during your day, unexpected revelation).

---

### BL-002: Onboarding Screen 2b — "Moments" Animation + Background SDK Init
**Pillar:** 0 (Onboarding)
**Status:** 🔲 BACKLOG (Concept only)
**Added:** July 29, 2026

**Feature Description:**
A candidate new screen (Screen 2 or an inserted "2b") placed right after Welcome, showing an animated/rotating list of the different things a "moment" can be — dreams, visions, random thoughts, processing fear, processing happiness, etc. — as a more visceral alternative or companion to the current static Education screen's 3-card list.

**Technical note (confirmed with Kell):** This cannot happen during the actual App Store download — the OS owns that phase (system download ring / App Store progress bar), and app code isn't running yet. The earliest a custom screen can appear is on first launch, which is where this would sit in the onboarding flow.

**Kell's idea:** Use this screen's on-screen dwell time to mask real background work — kick off SDK initialization / downloads (e.g., WhisperKit model, Supabase/Firebase init, any on-demand resources) behind the animation, so the delay feels intentional and on-brand instead of a blank loading spinner.

**Open questions:**
- Exact animation mechanic (crossfade list vs. carousel vs. something else)
- Whether this replaces Screen 2 (Education) entirely or sits alongside it as a new step (renumbers everything after it)
- Which SDKs/downloads actually need to happen at this point vs. lazily later

**Sequencing:** Revisit after the font/copy workshop for Screens 1–7 is locked (in progress as of this session).

---

### BL-001: "Dwellable Look Back" — Personalized Reflection Generator
**Pillar:** 9 (Post-Phase 2) or Pillar 7 Extension (Formation Intelligence)  
**Status:** 🔲 BACKLOG (Concept only)  
**Added:** May 8, 2026

**Feature Description:**
Generate personalized content (poems, declarations, narratives) that reminds user of:
- God's faithfulness throughout their journey
- Specific moods, themes, and experiences from their journals
- Emerging patterns and spiritual growth

**Output Formats:**
- Poem — artistic reflection on themes/moods
- Declaration — affirmation/statement about God's faithfulness in their story
- Narrative — story format ("This is who you are, Kell...") telling user's spiritual journey

**Powered by:**
- Formation Intelligence (Pillar 7) theme extraction
- LLM synthesis (Gemini/Mistral) using themes + moods as context
- User's actual journal content and captured moments

**Example Use Cases:**
1. User taps "Look Back" → sees themes from past 30 days (Hopeful, Breakthrough, Doubt)
2. User selects "Hopeful" mood → system generates poem about moments when they felt hopeful
3. User selects themes "God's Faithfulness" + "Trust" → system creates declaration or narrative

**Rationale:**
- Combat fatigue/burnout ("I hate Dwellable") with positive reminder content
- Shift from "journaling for self" to "journaling for spiritual growth"
- Use Formation Intelligence as key unlock for generative, personalized content
- Reinforce God's presence/faithfulness in user's lived experience

**Dependencies:**
- Pillar 7 (Formation Intelligence) themes fully locked and operational
- LLM model selected and integration complete
- Theme extraction and ranking system working

**Effort Estimate:** 
- Research & Design: 5 sprints (post-Phase 2)
- Implementation: 8-10 sprints
- Testing & Refinement: 3-5 sprints

**Phase:** Phase 3+ (Post-Phase 2 MVP)

---

### BL-002: Alternative Search/Discovery Views (Pillar 6 Extension)
**Status:** 🔲 BACKLOG  
**Added:** May 8, 2026  
**Phase:** Phase 3+

**Feature Description:**
Add multiple view modes for search & discovery beyond calendar + infinite scroll default.

**Requested Views:**
1. **Patreon-Style Content Display** — Journal entries displayed like Patreon creators showcase content (visual cards, featured posts, curated layout)
2. **Additional grid/card-based layouts** for browsing and discovery
3. **Theme-based browsing** (by Formation Intelligence themes)

**Rationale:**
- Default MVP view (calendar + scroll) is functional but may feel monotonous
- Patreon-style display makes user's own "story" feel more curated/celebratory
- Reinforces "God's faithfulness" narrative by presenting journals as meaningful content
- Discovery experience more engaging for long-term retention

**Dependencies:**
- Pillar 6 MVP (calendar + scroll) locked and working
- Formation Intelligence (Pillar 7) themes available for theme-based browsing

**Effort Estimate:** 5-7 sprints (design + implementation)

---

### BL-003: Batch Question/Analysis of All Reflections
**Status:** 🔲 BACKLOG  
**Added:** May 8, 2026  
**Phase:** Phase 3+ (Formation Intelligence Extension)

**Feature Description:**
Enable users to ask questions about ALL their reflections at once and receive synthesized insights across their entire journal history.

**Use Cases:**
1. "What has God taught me about trust?" → synthesis of all moments/journals touching on trust
2. "Show me my biggest breakthroughs" → ranked/filtered by theme/mood across entire history
3. "How have I grown spiritually?" → narrative summary of spiritual journey across all entries
4. "What themes keep appearing?" → frequency analysis + synthesis of recurring themes

**Powered by:**
- Formation Intelligence (Pillar 7) theme extraction
- LLM synthesis (Gemini/Mistral) for cross-journal analysis
- Aggregation of all user's moments + journals with thematic tagging

**Rationale:**
- Shift from "entry-by-entry reflection" to "holistic spiritual journey reflection"
- Use Formation Intelligence to surface patterns user might miss
- Powerful tool for spiritual growth and deepening faith awareness
- Complements "Dwellable Look Back" (BL-001) as insight/analysis tool

**Dependencies:**
- Pillar 7 (Formation Intelligence) fully operational
- Theme extraction, ranking, and cross-journal aggregation working
- LLM synthesis for multi-journal insights

**Effort Estimate:** 6-8 sprints (design + implementation + testing)

---
